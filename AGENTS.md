# Prototype Instructions

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

## Durable product decisions

- September 11 mobile modal frame reduction: remove another 16px around each side of the phone panel while preserving content size and centered placement. Phone width becomes `min(312px, calc(100vw - 64px))` and padding becomes zero, consuming the previous 16px inset. Content-sized height decreases by 32px; internal gaps and type/icon sizes remain unchanged. Desktop and tablet stay unchanged.

- September 11 mobile modal interior-padding follow-up: set phone panel padding to 16px on all four sides, superseding 52px vertical / 24px horizontal. Keep the existing responsive width, centered lockup, internal gaps, typography and controls. Desktop and tablet are unchanged.

- September 11 mobile modal visit-to-close refinement: reduce the phone-only gap from 44px to 36px. Preserve 52px top/bottom and 24px left/right panel padding, 36px visible close circle, its extended touch target, and desktop/tablet styling.

- September 11 modal close-circle size refinement: desktop (992px+) circle is 40×40px; phone (below 600px) circle is 36×36px. Keep tablet at 48px. Preserve the current X icon dimensions and 2px stroke. A transparent 6px extension around the phone circle preserves a 48px touch target without enlarging its visible fill. Keep internal gaps and the centered lockup.

- September 11 afternoon mobile modal alignment: use the accepted 464×536px desktop composition as the visual reference for the phone layout. Phone panel is `min(344px, calc(100vw - 32px))`, content-sized with 52px vertical / 24px horizontal padding; center the whole group. Globe is 62×34px, wordmark stays 24px, supporting text stays 12px, close circle stays 48px and X stays 12px with 2px stroke. Restore 44px between visit and Close to match the desktop hierarchy instead of the overly compressed 8px gap. Preserve other internal gaps and the true 8px visit-to-URL gap. This supersedes the prior phone 16px-inset / 328px-width trial; desktop and tablet remain unchanged.

- September 11 afternoon modal outer-space reduction: reduce standard panel dimensions from 528×600px to 464×536px, removing 32px of rendered space on every side while retaining the centered lockup and 400px content width. Standard CSS padding is 16px vertical / 32px horizontal (flex centering supplies the remaining vertical free space). Phone uses 16px padding on all sides instead of 48px vertical / 24px horizontal; side reduction is capped at a safe 16px inset because subtracting 32px would be negative. Phone width is `min(328px, calc(100vw - 48px))`, preserving its existing content width. Keep all internal spacing and element sizes unchanged.

- September 11 afternoon modal lockup centering: center the whole globe/wordmark/paragraph/visit/close group within the panel at every breakpoint. Use centered flex layout and symmetric vertical padding, removing the old -4px upper-content shift and auto close-button margin. Preserve all rendered internal gaps, including 60px visit-to-close standard and 8px phone, as explicit margins. Standard panel stays 528×600px; phone keeps content-sized height with 48px top/bottom padding. Preserve all type, icon, button sizes and horizontal padding.

- September 11 afternoon modal visit-to-close follow-up: reduce the gap by another 16px across all sizes (40px total reduction from the earlier layout). Standard close-button bottom margin is now 40px with its top margin auto; phone top margin is 4px with bottom margin zero. With the existing -4px text offset, the visible phone gap is 8px. Preserve button, icon and type sizes.

- September 11 afternoon modal visit-to-close spacing: reduce the existing visible gap by 24px at every breakpoint. In the fixed-height standard panel, reserve 24px below the close button to reduce its auto upper margin; in the content-sized phone panel, reduce the close button's top margin from 44px to 20px and leave its bottom margin at zero. Preserve text/icon/button sizes and other spacing. The shared -4px text offset means the measured phone gap is 24px (formerly 48px).

- September 11 afternoon modal close-circle follow-up: use a 48×48px circular close button across all breakpoints, superseding the previous 52px standard and 44px phone sizes. Preserve the 14px standard / 12px phone X, 2px stroke, fill, colors and spacing rules.

- September 11 afternoon modal close-icon refinement: reduce the X viewport by 2px at every breakpoint (standard 16×16px → 14×14px; phone 14×14px → 12×12px) and lighten its non-scaling stroke from 2.5px to 2px. Preserve the 52px standard / 44px phone circular buttons, placement, gray resting color and white hover state. Leave the globe unchanged.

- September 11 afternoon modal visit-gap final follow-up: increase the actual text-to-text gap from 2px to 8px across desktop, tablet and phone. Preserve `text-indent: 0` on the visit row; this supersedes earlier visit-gap values without changing paragraph spacing.

- September 11 afternoon modal scope clarification: the paragraph and visit-spacing refinements apply across all breakpoints. Standard paragraph uses 28px leading and 36px/28px upper/lower gaps; phone uses 20px leading and 24px/20px gaps (the same 4px leading and 8px gap reductions from its prior compact values). Font sizes remain unchanged. Reset inherited text indentation on the visit row to zero so its 2px flex gap is also the actual inter-text gap; the old inherited 16px indent made it appear 18px. This supersedes earlier instructions to leave phone paragraph spacing untouched.

- September 11 afternoon intro-modal visit spacing: use a 2px gap between “visit” and `www.frgmnt.ai` at every breakpoint, superseding the earlier 6px and 4px gaps. Preserve the URL, typography and other modal spacing.

- September 11 afternoon intro-modal paragraph spacing: reduce the standard wordmark-to-paragraph gap from 44px to 36px and paragraph-to-visit gap from 36px to 28px. Preserve the 28px paragraph line height, typography, panel geometry and close control. Phone spacing remains 32px above and 28px below the paragraph.

- September 11 afternoon intro-modal refinement: tighten the standard modal paragraph line height from 32px to 28px, preserving its 16px font size and surrounding spacing rules. Keep the phone override at 12px type / 24px line height. This is a targeted requested exception to the desktop freeze, not permission for other desktop refinements.

- September 11 phone detail interaction trial: below 600px, case-study and article URLs render only the selected story as a full-screen reading page with native document scrolling, not the complete continuous collection and not a nested-scroll dialog. Keep the selected piece's content and approved phone type scale, shared header, bottom Close control, deep links, browser history, and saved collection return position. Use a short restrained entrance/exit with reduced-motion support. Desktop (992px+) and tablet (600–991px) retain their existing detail presentation. This is a local reviewable implementation of Andrew's approved direction; do not treat visual acceptance or deployment as complete until explicitly confirmed.

- September 11 modal destination display correction: the modal visit row displays `www.frgmnt.ai` and links to `https://www.frgmnt.ai`. This supersedes the non-www address below. Preserve the lowercase presentation, 6px label gap, typography, colors, external-link behavior and surrounding geometry.

- September 11 modal destination correction: the modal visit row displays `frgmnt.ai` and links to `https://frgmnt.ai`, superseding the earlier Heavymental address. Preserve the lowercase presentation, 6px label gap, typography, colors, external-link behavior and surrounding geometry.

- September 11 desktop acceptance checkpoint: Andrew approved the complete current desktop experience as final for this phase and requested a pause in incremental desktop enhancements. Preserve the accepted Projects, Articles, History, detail, modal, navigation, typography, image and motion behavior. Subsequent work should focus on explicitly requested scopes rather than unsolicited desktop refinements.

- September 11 desktop masthead offset follow-up: shift the complete Projects, Articles and History navigation group an additional 32px left, changing its offset from the computed center between the logo and right content rail from -48px to -80px. Preserve the logo, menu gap, typography, states, vertical alignment, responsive geometry, mobile navigation and detail Back controls.

- September 11 modal upper-lockup position follow-up: shift the animated globe, `ZELLINGER` wordmark, three-line paragraph and visit row upward exactly 4px as one visual lockup. Keep the close button in its existing rendered position and preserve all internal gaps, element sizes, alignment and behavior.

- September 11 modal edge-stroke trial: add a very subtle 1px `#242424` inset stroke around the entire modal panel. Use an inset stroke so the approved 528×600px frame and all internal geometry remain unchanged. Preserve the panel gradient, 16px radius and 80% black backdrop.

- September 11 modal panel gradient refinement: replace the flat `#151515` panel fill with a restrained vertical gradient from `#151515` at the top to the original `#121212` at the bottom. Preserve the 80% black backdrop, typography, controls, spacing, geometry and motion. This supersedes the flat-fill correction below.

- September 11 modal panel fill correction: use `#151515`, the exact midpoint between the original `#121212` and the too-light `#181818` trial. Preserve the 80% black backdrop, typography, controls, spacing, geometry and motion. This supersedes the panel-fill refinement below.

- September 11 modal panel fill refinement: lift the introduction-modal panel subtly from `#121212` to `#181818`. Preserve the 80% black backdrop, typography, controls, spacing, geometry and motion.

- September 11 modal backdrop opacity follow-up: increase the black introduction-modal backdrop from 68% to 80% opacity. Preserve the panel, underlying carousel motion, interaction lock, entrance and dismissal behavior.

- September 11 modal globe-to-wordmark spacing follow-up: increase the vertical gap between the animated globe and the `ZELLINGER` wordmark from 16px to 18px. Preserve the globe and wordmark sizes, colors, animation, alignment, and all other modal spacing.

- September 11 modal close-circle scale follow-up: increase the circular close surface from 48×48px to the explicit 52×52px target. Preserve the 16×16px X, 8% white fill, muted-gray resting color, white hover color, center alignment and dismissal behavior. This supersedes the 48px circle size below.

- September 11 modal paragraph-to-visit spacing: reduce the gap below the three-line paragraph from 44px to 36px. Preserve the 44px wordmark-to-paragraph gap, the visit row's 6px internal gap, and all element sizes, colors and alignment. This supersedes the equal-gap centering trial below.

- September 11 modal corner-radius trial: increase the modal panel corner radius from 8px to 16px. Preserve the panel dimensions, fill, content geometry, motion and responsive behavior.

- September 11 modal globe scale trial: increase the animated globe exactly 15% from 72×40px to 82.8×46px. Preserve its muted-gray color, 2.8px internal strokes, 3.4px contour, 6.86s animation, center alignment, 16px wordmark gap and reduced-motion behavior.

- September 11 modal close-X scale: within the retained 48px close circle, use a 16×16px SVG for the X. Extend the diagonal paths close to the viewport edges so the visible mark reads at the intended scale. Preserve the 8% white circle fill, muted-gray resting color, white hover color, stroke treatment, placement and hit target.

- September 11 modal close-circle proportion: increase the circular close button and its 8% white accent fill from 38px to 48px while preserving the approximately 18px visible X, centered alignment, muted-gray resting color, white hover color and dismissal behavior. This supersedes the 38px close-target size in the entries below.

- September 11 modal close-button accent trial: use the existing semantic 38px circular close button as the wrapper for an 8% white background fill. Preserve the smaller muted-gray X at rest, white-only hover color, borderless treatment, placement, hit target, focus outline, scale response and dismissal behavior.

- September 11 modal horizontal compaction trial: reduce each desktop side inset from 80px to 64px and reduce the panel width from 560px to 528px. This preserves the established 400px content measure and all internal alignment while making the modal 32px narrower. Together with the vertical compaction below, the current desktop panel is 528×600px.

- September 11 modal vertical compaction trial: reduce the modal panel height from 640px to 600px by removing 24px above the globe and 16px below the close target. Use 96px top and 84px bottom padding while preserving the 560px width, 80px horizontal padding, every inter-element gap, element size, alignment and ordering.

- September 11 modal close-icon refinement: preserve the borderless transparent 38px close hit target and its lower-center placement, but render the visible X at approximately 18px using a 24px SVG. Its resting color matches the modal muted gray `#8a8a8a`; only pointer hover turns it white. Preserve the existing focus outline, active scale and dismissal behavior.

- September 11 modal paragraph centering: use the same 44px vertical gap from the modal wordmark to the three-line paragraph and from the paragraph to the visit row. This replaces the asymmetric 64px/24px spacing while preserving the visit row's established vertical position.

- September 11 modal wordmark scale trial: render the centered `ZELLINGER` modal wordmark at 32px with a matching 32px line height, Geist Bold 700 and `.04em` tracking. Preserve its color, alignment and 16px globe gap. This supersedes the 26px/26px scale and `.02em` tracking recorded in the literal-reference entry below.

- September 11 modal small-copy refinement: the three-line muted-gray modal body copy, the gray `visit` label and the white `www.heavymental.ai` link all use Geist Medium at 16px. The visit label and URL use exactly a 6px flex gap with no additional inter-item padding or margin. Preserve their existing 32px line height, colors, alignment, wording and remaining spacing. This supersedes the 18px size and wider visit-row gap recorded in the modal reference entry below.

- September 11 Projects hover stability: project-card title, metadata and description lockups remain stationary and retain their authored colors on pointer hover. Preserve the full-card click target, keyboard focus treatment, Articles hover response, collection carousel behavior and collection-to-detail transition.

- September 11 Projects introduction modal literal reference: on the Projects home route only, show a once-per-browser-session overlay centered over the visible landing screen. Match Andrew's supplied screenshot as a 560×640px almost-black `#121212` panel with an 8px radius and 80px horizontal insets. Center a 72×40px animated muted-gray `#8a8a8a` globe 16px above a white `ZELLINGER` wordmark in Geist Bold 700 at 26px/26px with `.02em` tracking. Place the exact three-line placeholder `Lorem ipsum dolor amet, consectetur / adipiscing elit. Aenean et sapien augue. / ipsum dolor amet consectetur.` 64px below the wordmark in the same muted gray, using Geist Medium at 18px/32px. Place a centered lowercase gray `visit` label and white `www.heavymental.ai` external link 24px below the copy. Use a bare white 21px-visible X drawn inside a borderless transparent 38px close target near the lower center. The blue frame outline and gray exterior visible in the reference are Figma selection chrome, not part of the modal. The globe uses 2.8px internal strokes and a 3.4px outer contour; its clipped SVG meridians rotate horizontally on a continuous 6.86s linear cycle, the outline and latitudes stay fixed, animation pauses offscreen and in hidden tabs, and reduced-motion users receive a balanced static globe. Keep the automatic Projects carousel running continuously behind the modal and after dismissal; the modal must never register a shared motion-pause reason. While the modal is mounted, suppress wheel, single-touch move and scroll-navigation keys before they reach the page or custom carousel input runtime; native overflow remains locked. Remove those input blockers with the modal so manual scrolling resumes immediately after dismissal. Keep the background interaction-inert, and support dismissal by the close control, Escape, or the backdrop. Use a restrained 240ms opacity and 8px vertical entrance, restore focus on close, and resolve instantly under reduced-motion preferences. Do not show the modal on Articles, History, case-study details, or article details.

- September 10 image corner-radius system: every live image surface uses the shared `--media--image-radius` token at 8px. Apply it to both native image elements and the CSS-background media holders used by Projects, case-study details and History, including their transition clones. Preserve image dimensions, aspect ratios, crop positions, accessibility, carousel geometry and motion behavior.

- September 10 editorial leading refinement: use a restrained relaxed-leading scale across the text-heavy reading surfaces. History's large introduction lead uses 1.3 line height; History practice, prose, client-list and Work With Me copy use 1.7; Article Detail paragraphs, lists and blockquotes use 1.7; Case Study Context, Key Decisions and Outcome copy use 1.8. Preserve the existing font sizes, text measures, paragraph margins, section gaps, headings, metadata, collection-card typography and motion behavior.

- September 10 quiet text-motion system: page-title SplitText remains the site's only character-level signature motion. Project and Article collection copy responds as one restrained 4px horizontal settle with a 30ms title-to-metadata-to-description cadence; metadata and descriptions brighten while titles remain white. Project and Article detail sections reveal once as authored label/body groups with a 10px rise, 50ms internal stagger and 440ms maximum body duration on desktop and compact layouts. History uses the same grouped reveal only in compact native-scroll layouts; its desktop continuous rail remains unlayered. Masthead links use a 160ms color response, desktop Back arrows nudge 3px toward their destination, and History contact links shift 4px on hover or keyboard focus. Preserve the page-title cascade, collection-to-detail shared transitions, continuous carousel behavior, direct-scroll hover behavior, reduced-motion instant state, semantic text grouping and existing typography. Do not add paragraph word/line splitting, blur, tracking animation, decorative loops or a second signature gesture.

- September 10 project restoration: restore Obagi (`obagi`), Gero (`gero-app`) and NW Mutual (`northwestern-mutual`) to the public Projects collection, automatic motion loop, circular detail sequence and generated detail routes by emptying `HIDDEN_PROJECT_SLUGS`. Preserve their original canonical records, narratives, metadata and positions. Use Andrew's revised `Project-Images/obagi-screen.png`, `gero-screen.png`, and `nwmutual-screen.png` as their browser-served media while retaining the established filenames, shared collection/detail mappings, accessibility labels, 3:2 frame, cover crop and transition behavior. This supersedes the earlier September 10 reversible project visibility decision.

- September 10 Live Auctioneers and Positive Intelligence image refresh: use Andrew's revised `Project-Images/liveauctioneers-screen.png` and `Project-Images/positiveintelligence-screen.png` as the browser-served media for their existing canonical projects. Preserve the established filenames and shared collection/detail mappings, source files, project order, copy, accessibility labels, 3:2 media frame, cover crop, and carousel/detail behavior.

- September 10 project-image refresh: use Andrew's revised `Project-Images/andrew-eccles-screen.png`, `fi-screen-array.png`, `mcdonalds-kiosk.png`, `modern-age-screen.png`, `proctor-screen.png`, `pwc-website.png`, and `wework-screen.png` as the browser-served media for their existing canonical projects. Preserve each established filename and shared collection/detail mapping, the source files, project order, copy, accessibility labels, 3:2 media frame, cover crop, and carousel/detail behavior.

- September 10 History portrait crop: enlarge the figure within the existing History portrait frame by setting the background crop to 120% of its natural fitted size, superseding the earlier 112% crop. Preserve the centered crop, 3:2 frame, shared 90% desktop width, full-width compact layout, source image, accessibility label, carousel order and motion, and right-rail hover/manual-scroll behavior.

- September 10 History portrait measure: at desktop widths of 992px and above, the History portrait uses the same shared 90% right-rail measure as the text sections beneath it, preserving their common left edge and uniform right edge. Compact layouts remain full width. Preserve the 3:2 crop, source image, accessibility label, carousel order and motion, section spacing, and right-rail hover pause.

- September 10 right-rail hover pause: on hover-capable desktop pointers, entering the active Projects, Articles, or History right-rail carousel pauses only its automatic GSAP loop at the exact visible phase. Wheel and keyboard input continue to move the hovered carousel directly, without inertia or an automatic coast. Leaving resumes ambient motion from the manually adjusted phase unless another pause state remains active. Keep page scrolling and independent motion controls available. Preserve touch behavior, keyboard-focus pausing, user pause state, reduced-motion behavior, direction, speed, carousel geometry, transitions, and teardown cleanup.

- September 10 History Experience density: at desktop widths of 992px and above, the Experience register uses a 6px row gap with padding-free rows. Preserve its three-column geometry, typography, shared 90% content measure, section spacing, carousel motion and ordering, plus the 16px mobile and tablet row gap. This supersedes the earlier 10px desktop row-gap decision.

- September 10 History right-rail measure: at desktop widths of 992px and above, the History introduction, every labeled editorial block, Experience section and Contact section share one 90% content measure within the right rail. Their left edge remains aligned with the portrait and rail; their right edge is uniform and tighter than the full-width portrait. Nested prose and Experience content fill that shared section measure without compounding the 90% reduction. Compact layouts remain full width. Preserve the portrait geometry, typography, spacing, carousel motion and section order.

- September 10 Geist Mono scale unification: every live Geist Mono treatment uses the same shared 11px size as project and article metadata. This includes the clock, collection metadata, project and article detail metadata, project section tags, article section headings, History labels and History dates at every breakpoint. Preserve each treatment's existing color, weight, line height, tracking, case, spacing, alignment and motion; `--typography--mono-size` is the single size source.

- September 10 neutral focus highlight: the shared keyboard-focus outline uses neutral grey (`#9c9c9c`) instead of brand orange across article and project cards, Masthead links, buttons and other elevated-theme controls. Preserve the 2px outline, existing route-specific offsets, full-card focus area, keyboard accessibility and all hover/current-page treatments.

- September 10 article-title transition parity: Articles collection titles and all Article Detail titles use the same 18px size at desktop widths of 992px and above, including incoming titles in the circular detail sequence. This removes the collection-to-detail size jump. Preserve the 16px compact size, font family, weight, line height, tracking, text measure, lockup order and transition choreography.

- September 10 History left-column inset: at desktop widths of 992px and above, the History page's complete left column reserves 48px of interior space at its right edge. Apply the same inset to the page-title container and the corresponding biography grid track. Preserve the column boundary, left alignment, right content rail, title typography, desktop navigation offset and all compact layouts.

- September 10 desktop masthead offset refinement: shift the complete Projects, Articles and History navigation group 48px left from its previously centered desktop position. Preserve the logo, menu gap, typography, states, vertical alignment, responsive geometry, mobile navigation and detail Back controls.

- September 10 History contact refinement: remove X.com from the live History contact links and public biography contact data. Preserve email, LinkedIn, GitHub, and Cal.com in their existing order, styling, targets, spacing, wrapping, hover and keyboard-focus behavior. This supersedes the earlier History contact-links decision that included X.com.

- September 10 History portrait reversion: restore `public/images/history/az-headshot-extended-v1.png` as the live History portrait, including its profile accessibility label, counterflow preload, 3:2 crop, frame, carousel motion, and responsive geometry. Retain `az-headshot-extended-v2.png` as an inactive archived frontal alternative; do not delete or mount it without a later explicit request. This supersedes the earlier September 10 frontal portrait replacement decision.

- September 10 History portrait replacement: History uses `public/images/history/az-headshot-extended-v2.png`, a 3:2 horizontal outpaint of Andrew's supplied frontal headshot. The original square headshot is preserved exactly in the centered 1024px region; generated pixels are limited to the lateral gray studio-background extension. Keep the figure more prominent than the previous profile portrait, with modest headroom and balanced side space. Preserve `az-headshot-extended-v1.png` as an inactive earlier version, plus the History frame, crop behavior, carousel motion, accurate portrait accessibility label, and responsive geometry.

- September 10 Experience register refinement: History organization names use their authored title casing, preserving intentional brand acronyms. At desktop widths of 992px and above, the register spans 90% of its rail with 10px between rows, 16px between columns, and tighter column proportions. Preserve role and date typography, source content and ordering, the existing phone layout, section spacing, and History carousel behavior.

- September 10 Select Clients refinement: the live History client register contains exactly these 28 user-approved names in this order: Adidas, Adult Swim, Amazon, American Express, Apple, Audible, Avantos, Cosmos, Fi, Foursquare, Google, IBM, Instrumental, Live Auctioneers, McDonald's, Mercedes-Benz, MetLife, Microsoft, Modern Age, NBCUniversal, Noom, Northwestern Mutual, Procter & Gamble, PwC, Seattle Genetics, Thomson Reuters, Turner Media, WeWork. Render the names in their authored title casing, preserve intentional brand acronyms, and separate names with a comma plus space. Keep the Select Clients label uppercase mono and preserve the list's size, color, line height and section spacing.

- September 10 History positioning narrative: the approved Lead identifies Andrew as a hands-on product design lead spanning complex workflows, design systems and AI products, then defines this site as the standing record of the commercial design career that established his practice. The single Practice paragraph preserves his cross-disciplinary history and explicitly explains that the past two and a half years of AI product building and automation consultancy work are newer, unrepresented extensions of the same foundation. Keep the former An Evolving Practice section removed. Preserve Experience and subsequent History ordering, typography, content width and carousel behavior.

- September 10 Work With Me positioning: state explicitly that Andrew is open to senior or lead product design roles, especially where complex workflows, design systems, AI and hands-on product development intersect. Present both establishing design for an early team and contributing within an established organization as valid paths. Preserve the approved two-sentence wording, section position, typography, contact block and History motion behavior.

- September 10 History tools removal: remove the complete live Tools & Technologies section and its reserved spacing from History. Work With Me follows Select Clients using the shared 64px desktop/tablet and 48px phone section rhythm. Preserve the tools records as inactive source data for possible future reuse, plus all other History content, ordering, typography and carousel behavior.

- September 10 History capability removal: remove the four live History capability subsections AI Systems, Product, Delivery, and Direction as one complete presentation block. Keep Select Clients as its own normal History section using the shared 64px desktop/tablet and 48px phone section rhythm. Preserve the underlying capability records as inactive source data, plus all other History content, ordering, portrait, typography and carousel behavior.

- September 10 History portrait refinement: remove the colorized `A.` overlay from the live History portrait so the source photograph displays without typographic artwork. Preserve the portrait image, crop, dimensions, carousel behavior and responsive layout; keep the fixed-artwork implementation archived and inactive for possible future reuse.

- September 10 full-row article links: each Articles collection anchor spans the complete horizontal listing width and owns its trailing 76px desktop or 32px compact spacing, making the full allocated listing rectangle clickable. Keep list items padding-free, preserve the exact visual rhythm, text-only lockup, 60ch content measure, semantic single-link structure, carousel geometry and collection-to-detail transition source.

- September 10 article-section rhythm: Article Detail uses 48px between adjacent authored sections on desktop and tablet, increased from 32px, while phones retain 32px. Keep the first section flush, preserve the 16px heading-to-body gap, paragraph and list rhythm, 64px header-to-body gap, 148px inter-article gap, motion choreography and article copy.

- September 10 article-detail opening inset: on desktop, both click-through and direct-loaded Article Detail views settle the selected article header 64px below the physical top of the viewport instead of at the 16px project-detail inset. The collection-to-detail text transition must resolve into that same position. Preserve the 16px desktop project-detail inset, the existing 80px compact detail inset, article boundary sequencing, reading-anchor restoration, typography and text measure.

- September 10 Articles summary density: expand each source-supported article summary toward the desktop two-line capacity without inventing claims. On the Articles collection at 992px and above, allocate exactly two lines, clamp overflow with an ellipsis, and use 76px between listings, adding 16px to the previously approved 60px rhythm. The complete summary remains available in article detail headers. Preserve the 60ch shared measure, title/meta spacing, vertical lockup order, 32px compact listing gap, carousel behavior and collection-to-detail transition geometry.

- September 10 article-detail boundary reveal: while scrolling between article detail units, reveal the incoming title lockup in the lower viewport entry zone rather than delaying full opacity until the canvas midpoint. Begin the title, metadata and summary cascade as the header crosses the bottom edge; resolve the complete lockup by approximately 75% viewport height, before it enters the main reading area. Keep the motion continuously tied to scroll and reversible, with smaller vertical offsets than project lockups. Preserve article spacing, typography, body position, circular sequencing, compact/reduced-motion behavior and all project-detail boundary choreography.

- September 10 unified Articles reading measure: the text-only article collection lockup, article detail header, and article body share one left-aligned 60ch maximum measure inside the existing right rail. At every breakpoint, both lockups stack title, metadata, then description vertically with 16px from title to metadata and 8px from metadata to description. Use the detail header as the collection-to-detail transition destination. Preserve typography, carousel position and behavior, long-form copy, and responsive rail insets. This supersedes earlier 68ch article-detail measures.

- September 10 text-only Articles system: article collection and detail views do not render assigned imagery or `Read more`; retain `ARTICLE_IMAGES`, the article media records, and the image assets as a reversible archive. The collection uses a vertical title/metadata/summary lockup with 18px desktop titles, 16px compact titles, mono metadata, summaries, and 76px desktop/32px compact row gaps. Article details use the same vertical lockup with a 16px title, omit the hero and divider, place the long-form body 64px below the header, and render list markers white. Collection-to-detail motion carries the selected text lockup into the detail header; preserve compact and reduced-motion fallbacks.

- September 10 reversible project visibility: hide Obagi (`obagi`), Gero (`gero-app`) and NW Mutual (`northwestern-mutual`) from the Projects collection, its motion loop, the project-detail sequence and generated detail-route aliases. Preserve all three canonical records, narratives, metadata and media in `ALL_PROJECTS`; public `PROJECTS` is the filtered set. Restore a case study by removing its slug from `HIDDEN_PROJECT_SLUGS`, not by reconstructing deleted content.

- September 10 case-study decision-list measure: Key Decisions lists occupy 90% of their containing body column and remain left aligned. Preserve bullet color, indentation, type, item spacing, copy, the label/body grid, article lists and all other responsive behavior.

- September 10 collection-carousel edge passage: on motion-capable desktop layouts, the Projects, Articles and History right-rail carousel viewports run from the physical top to the physical bottom of the browser so content moves past the screen edges instead of being clipped at the 24px canvas inset. Preserve each route's horizontal rail bounds, content, spacing and motion, plus compact, reduced-motion, detail and non-carousel layouts. This expands the earlier Projects-only edge-passage decision.

- September 10 desktop case-study widow refinement: at desktop widths of 992px and above, project narrative paragraphs use `text-wrap: pretty`. When a rendered paragraph still leaves a single word stranded on its final line, tighten only the affected, source-supported wording; do not invent claims or alter project meaning. Mobile and article typography remain unchanged.

- September 10 project-boundary lockup stability: while scrolling between project detail units, the outgoing project's title, tags and description remain stationary and fully opaque. Natural document scroll alone carries the lockup away. Preserve the outgoing and incoming media choreography, the incoming lockup reveal, reverse scrolling, compact layouts, reduced-motion behavior and article-detail behavior.

- September 10 case-study narrative consolidation: project detail views have no divider between the project lockup and narrative. Merge each existing Context and Work passage, in that order, into one paragraph under Context; retain Key Decisions and Outcome where present, without inventing missing evidence for concise engagement records. Context, Key Decisions and Outcome labels match project metadata at 12px/13px mono with `.36px` tracking. Project decision bullets are white. Use a direct 64px summary-to-Context gap with no former 52px divider inset, then 48px between sections on desktop and 32px on compact layouts. Article detail rules, lists and typography remain unchanged.

- September 10 project-title simplification: the 19 canonical display titles, in collection and circular-detail order, are Avantos, Amazon, Audible, Turner Media, Obagi, WeWork, Google, Live Auctioneers, Andrew Eccles, P&G, Modern Age, Fi, Thompson Reuters, Gero, Foursquare, PwC, NW Mutual, McDonalds, and Positive Intelligence. Apply these shared titles to collection cards and project detail headers without changing slugs, routes, metadata, headlines, descriptions, media labels, project order or case-study body copy.

- September 10 desktop Back-control refinement: desktop detail Back controls use 16px medium gray (`#aaa`) text with a matching 16px left-arrow icon; hover resolves to white. Preserve the 44px hit target, placement, 10px icon gap, focus treatment, return behavior and compact Close controls. This supersedes the earlier 18px orange Back-arrow treatment.

- September 10 Projects-carousel edge passage: on motion-capable desktop layouts, the Projects carousel viewport runs from the physical top to the physical bottom of the browser so cards move past the screen edges instead of being clipped at the 24px canvas inset. Preserve the right-column horizontal bounds, 40px card gap, card geometry, content and motion, plus compact, reduced-motion and non-Projects layouts.

- September 10 Projects-card rhythm refinement: Projects collection titles use 18px at desktop widths and retain 16px on compact layouts; case-study detail titles remain 16px. Across the Projects collection, use 8px from tags to description, 32px from image to text lockup, and 40px between projects. Preserve the title's family, weight, tracking and casing, plus the existing grid split, metadata treatment, descriptions, media geometry and case-study detail spacing. This supersedes the collection-title and collection-spacing portions of the earlier project-card decision.

- September 10 desktop masthead alignment: center the complete primary-navigation menu within the horizontal space between the rendered logo's right edge and the right content column's left edge at desktop widths. Derive the position from live logo, menu, viewport and grid-gutter geometry so it remains centered across desktop sizes and font loading. Preserve link spacing, typography, states, destinations, vertical alignment, compact navigation and detail Back controls.

- September 10 desktop page-title refinement: every shared page-level title uses 80px at widths of 992px and above across Projects, Articles, History and their detail routes. Preserve the existing weight, line-height ratio, tracking, position, title transitions and compact/tablet sizing. This supersedes earlier 124px and 72px desktop page-title decisions.

- September 10 project-card typography and metadata refinement: Projects collection titles and case-study project titles use a fixed 16px size at every breakpoint. Visible project tags include role and year only; retain client metadata in the canonical project records for provenance, but do not project it into collection cards or detail headers because the project title already supplies that identity. Preserve project title casing, font family, weight and tracking, plus article typography and metadata.

- September 10 selected-navigation refinement: remove the orange dot beneath the selected Masthead item at every breakpoint. The current item is indicated only by white text, while inactive items retain their existing gray treatment. Preserve navigation typography, spacing, destinations, hover states, focus indicators and detail Back controls.

- September 10 page-title lockup removal: remove the complete supporting lockup beneath every page-level title on collections and detail views, including the ordinal counter, current item or section name, and progress rule. Do not retain hidden lockup DOM, reserved spacing, tracking observers or transition treatment. Preserve page titles, desktop Back controls, content rails and navigation behavior.

- September 10 identity and clock refinement: the shared native logo reads `ZELLINGER` on every route and detail view. The persistent `America/New_York` clock displays only `HH:MM:SS`, with no `NYC` prefix, and uses a 13px font size, increased by 1px from 12px. Preserve the existing logo and clock typography, placement, colors, links, timezone and one-second live updates.

- September 10 repository grooming: the canonical repository is `andrewzellinger.com`. Keep shipped pages and asset URLs free of predecessor branding. Preserve imported runtime bytes, third-party notices, approved visuals, content and motion. Use `npm run check` for regression/build verification; archival mirroring is not routine maintenance.

- September 10 Back arrow refinement: desktop detail Back arrows use an 18px square, reduced from 20px. Preserve label typography, orange color, spacing and the 44px click target.

- September 10 article divider refinement: article-detail dividers occupy the same first grid track as article body text, rather than spanning the full right rail. Preserve image widths and existing vertical spacing.

- September 10 context restoration: show the existing context text and progress indicators on Projects, Articles and History main pages as well as detail views. Remove the Masthead experiment's visibility suppression; preserve the original desktop layout, live progress logic and compact/short-screen hiding rules.

- September 10 article-detail desktop measure: title, metadata and description share the body's 68ch reading-column width (measured in the same 13px Geist parent font). Keep text left aligned and image/divider full-width. Collection cards and compact layouts remain unchanged.

- September 10 desktop Back spacing: reduce the control-to-page-title gap from 16px to 8px; retain the 44px hit area, left alignment, and compact return controls.

- September 10 phone Experience layout: company spans the full first row; role and date share the next row in flexible-role and auto-date columns, with a 16px horizontal gap and existing 8px internal row gap. Desktop/tablet are unchanged.

- History spacing: desktop contact links have 48px horizontal gaps, including email-to-socials. Portrait-to-opening-text padding is 64px desktop/tablet and 48px phone. Smaller-screen contact spacing remains unchanged.

- History contact links: show LinkedIn (https://www.linkedin.com/in/andrewzellinger/), GitHub (https://github.com/a2z2k26), and Cal.com (https://cal.com/andrewzellinger) to the right of email, wrapping when needed. X.com is intentionally omitted. All contact links are white without underlines, including hover; retain keyboard focus indicators.

- Desktop Masthead alignment: center menu labels vertically with the logo and maintain exactly 156px between the logo's right edge and the first menu link, superseding the 48px trial. Recompute from actual logo dimensions on font load and resize. Compact navigation remains unchanged.

- History opening-copy refinement: the lead paragraph spans 90% of the right content column on desktop (992px+), retaining full width on smaller screens. The gap to the following practice paragraph uses the shared section gap (64px desktop/tablet, 48px phone).

- History spacing refinement: all section heading-to-content gaps use 24px; main section separation is 64px desktop/tablet and 48px phone. Preserve 16px Experience entry gaps. Tools must not double its parent heading gap. Contact follows the same section gap and ends with 80px bottom padding, plus compact safe-area inset.

- History list typography: Select Clients and Tools & Technologies content lists use uppercase white Geist Sans, preserving their existing size and spacing. This applies to the list text, not a change to section headings.

- September 9 History ordering and density: Education immediately follows Experience. Experience entries use a 16px inter-entry gap instead of 24px; preserve typography, columns and within-entry mobile spacing.

- September 9 History tools presentation: Tools & Technologies has no preamble or category subheadings. Combine every existing tool in source order into one dot-separated, full-width list matching Selected Clients typography and spacing.

- September 9 History label refinement: Design Practice uses white uppercase Geist Sans, preserving its existing 12px size, weight, tracking and spacing. Other History labels are unchanged.

- September 9 canonical Masthead acceptance: Andrew approved promoting the Masthead navigation and its refinements to main and removing unused navigation directions. The sole live implementation is src/site-navigation. Desktop has persistent header links even on detail pages; orange Back sits left aligned 16px above the large title, with the existing reading lockup restored beneath desktop detail titles. Tablet retains the black dock and orange detail-close circle. Phone uses the 68px black tab bar with white active/gray inactive labels and orange dot; detail replaces it with white Close text and a 14px orange X. Remove the concept selector and its reserved mobile spacing. Preserve all unrelated content and motion. GitHub publishing requires a separate request.

- September 9 detail lockup restoration: show the existing counter, current piece title and reading-progress rule below the large page title on desktop case-study and article detail views. Retain existing typography, spacing and live reading progress; keep collection lockups and compact/short-screen lockups hidden. Back remains above the heading.

- September 9 desktop Back placement supersedes the below-title trial: place the 44px-high orange Back control above the page title, left aligned, with 16px from the control's bottom edge to the title's top edge. Mobile and tablet remain unchanged.

- September 9 detail Masthead refinement: keep desktop Masthead links visible in both detail families. Desktop return control becomes an orange left-arrow and Back label, left aligned 32px below the page title, replacing the bottom circle. Phone Close bar keeps white text but uses a smaller 14px orange X. Tablet orange circle remains unchanged. Preserve shared return behavior and keep changes isolated from main.

- September 9 mobile detail navigation: below 600px, replace the floating orange detail-close circle with a full-width black bottom-bar state matching the 68px Masthead navigation plus safe area. Show a centered white X and Close label, returning to the originating collection. Desktop and tablet retain their orange circle and approved positions.

- September 9 mobile Masthead spacing: increase phone bottom-bar vertical padding to 12px per side (68px total before safe area), retaining the existing type, orange indicator and black palette. Tablet and desktop remain unchanged.

- September 9 Masthead compact palette: tablet dock and mobile bottom navigation use a solid black background, white current/hover text, gray inactive text, and the existing orange current-page dot. Preserve geometry and the separate orange detail-close circle.

- September 9 Masthead refinement: Andrew selected Masthead as the working direction with exceptions. Case-study and article-detail Close is an orange circular X control, fixed at the bottom center of the right column on desktop (75% viewport width) and bottom center of the viewport below 992px. Keep the existing parent-collection return behavior. Initial sizing is 64px with a black 24px X and a 24px safe-area-aware bottom gap. This remains isolated from main.

- September 9 six-concept navigation lab: Andrew rejected the vertical up/down arrangement and requested six additional interactive concepts with deliberate desktop/tablet/mobile adaptations. The isolated worktree now explores Masthead, Chapter Index, Title Picker, Edge Tabs, Visual Overview and Command Menu, selected through a preview-only chooser. Preserve hidden progress lockups, existing content/media/route motion and a separate parent-collection Close on detail views. These are comparison prototypes, not approved replacements; main, GitHub and port 5173 remain untouched. Do not restore the rejected sequential controls as an active option.

- September 9 vertical navigation arrangement: Andrew approved trying Previous above the page title with an up-chevron and Next below it with a down-chevron, both at the title's left edge. Preserve 44px orange circles, 32px desktop / 24px compact title gaps, the three-page cyclic destinations, hidden progress lockups and separate Close behavior. Desktop title position stays unchanged; compact adds room above its title for Previous. This supersedes the side-by-side row only in the isolated navigation demo; main remains untouched.

- September 9 circular sequential navigation: hide the complete page-progress lockup (counter, item/section name and progress line) on every route in the demo. Previous / Next are now 44px circular brand-orange buttons with black directional chevrons and accessible destination names beneath the unchanged page title. The separate Close control keeps its existing white text-button treatment and correct parent-collection return. This supersedes visible progress stacks and text-labeled Previous / Next in the preceding experiment entry; main remains untouched.

- September 9 sequential-navigation revision: Andrew rejected both Direct and Reveal demos. Replace them in the isolated navigation worktree with Previous / Next buttons beneath the page title, cycling through Projects, Articles and History in both directions. Detail pages replace the pair with a Close button that returns to their own collection through the existing return lifecycle. Keep the accepted desktop title fixed; put the 44px row 32px below it and the existing context stack 32px below the buttons. Compact collection controls follow the title in normal flow; the proposed compact detail Close is bottom-fixed so native entry/reading cannot scroll it out of reach. No comparison toolbar, sheet or circular menu is mounted. Preserve the main checkout and port 5173; this revision remains an unapproved preview on port 5180, not a merge or publication.

- September 9 navigation-mechanics demos: Andrew authorized two interactive alternatives only in `codex/navigation-mechanics-demo` and its isolated worktree, based on main at `763d35c`. Demo 01 is persistent Projects / Articles / History text navigation in the left column with an active marker. Demo 02 uses a Menu disclosure that takes over the left column with oversized section links while keeping the right rail visible; compact layouts use a full-screen disclosure. Both separate detail return from global navigation and preserve current content, typography, media, route transitions, carousel behavior, keyboard support and reduced motion. Demo controls switch between both without resetting reading position. Keep main, GitHub main and port 5173 untouched; run the experiment separately. Neither alternative is accepted for merging or publishing yet.

- September 9 final synchronization approval: Andrew explicitly requested committing all outstanding site refinements and article-artwork assets to `main` and pushing to GitHub. The current reviewed implementation is the accepted baseline, summarized in `docs/accepted-design-baseline.md`; older experimental palette, typography and layout entries remain historical where superseded. Include the original artwork, unused studies and assignment inventory without activating unselected images. Verify a clean canonical working tree and identical local/GitHub `main` commits after pushing; do not force-push, delete unrelated branches/worktrees or infer a production deployment request.

- September 9 final desktop title-size adjustment: reduce the shared page-level heading size from 132px to 124px at widths of 992px and above. Change only font size; preserve weight 500, tracking, line-height ratio, position, transitions, context spacing, compact sizes and all other typography. This supersedes the 132px entry below.

- September 9 divider-free History: remove all divider rules from the History editorial content, including Experience row boundaries, the Selected clients separator and contact-link bottom rules. Use spacing rather than lines to separate content: 24px between unpadded Experience rows, 24px below its heading, 48px between major sections (32px on phones), 16px label/prose and paragraph gaps, and 24px capability-grid gaps. Selected clients has one 48px separation from the capability grid, without stacked border padding. Keep the portrait-to-lead gap, existing typography, two orange text accents, complete factual copy, nine-section context/progress, loop velocity and 64px carousel seam unchanged. Contact top spacing is 32px with no extra desktop bottom padding; preserve compact menu/safe-area clearance. This supersedes earlier History divider and spacing rules.

- September 9 duplicate-image correction: the thirteen files in `Article-Image/` contain twelve distinct artworks. `mixed-material-outtakes__company-of-one__option-4.png` duplicates the reference artwork used by The Constraint Was the Brief. Keep the latter assignment and change Showing My Teeth to `material-sculpture-set__you-always-let-yourself-win__option-2.png`. Eleven distinct artworks are assigned; only `two-dollar-bill__option-2.png` remains genuinely available. Preserve the redundant original but mark it as a duplicate, not a spare, in the folder README. This supersedes the two-spare count below.

- September 9 article artwork assignment: Andrew approved assigning eleven distinct abstract images from `Article-Image/` to the eleven articles, without requiring subject-matter correspondence. Keep the same assigned image on each collection card, detail hero and transition; preserve article order, text and 3:2 geometry. Originals remain unchanged. The shared mapping is `src/article-images.js`, with optimized WebP derivatives in `public/images/articles/`. `Article-Image/README.md` documents every assignment and the two unused originals reserved for future articles; update that inventory whenever assignments change.

- September 9 History carousel: the complete History right column now uses the same desktop GSAP loop as Projects and Articles. Continue the upward route sweep into 60px-per-second upward autoplay; wheel/touch/keyboard input can reverse direction and use the shared acceleration and eased decay. Loop the full biography and contact section with a 64px seam gap, not individual text sections. Track the nine canonical History sections and progress by visible loop position, including reverse travel and wrapping. Preserve the static A-dot portrait artwork in each visual copy, native contact-link behavior, keyboard focus, saved user pauses and transition/visibility pauses. Keep tablet/mobile and reduced-motion History in native document flow, and do not change the approved menu, typography or content.

- September 9 article-body compaction and alignment: Andrew authorized selective paragraph consolidation and tighter article formatting to reduce reading height. Merge only connected prose in the authored source, not by automatic paragraph counts; preserve wording, section headings, lists, quotes, metadata and order. Use 15px body text with 24px leading, 16px paragraph/heading-to-copy gaps, 32px section gaps and 6px list-item gaps. The opening divider remains full rail width, with 48px above and 32px below. Anchor the bounded 68ch reading column to the right rail's left edge, matching the image/header rather than centering it. Remove every Related work link and its record mapping; these were not approved and must not return without explicit direction. Preserve collection cards, case-study styling, navigation and scroll behavior.

- September 9 geometric material-sculpture set: Andrew requested another 20 article-image options in the approved photoreal material-sculpture language, with increased emphasis on shape and geometry. Favor crisp primitives, nested forms, apertures, repeated modules, tangencies, wedges, arcs, planes and interlocks while preserving believable fabricated materials, joins, pressure, gravity, imperfections and studio shadows. Keep this exploration isolated under `Project-Images/article-options/material-sculpture-set-4/` pending article-by-article selection; do not publish an active image yet.

- September 9 latest desktop title size: increase all page-level titles, including collection/detail shells and History, from 116px to 132px at widths of 992px and above. Preserve Geist Medium 500, .98 line height, -.035em tracking, left alignment, the 32px upward offset from true vertical center, character transitions and 32px context gap. Tablet/mobile, content headings, logo and menu remain unchanged.

- September 9 collection-return autoplay: Projects and Articles must resume ambient carousel motion after closing detail or returning with browser Back, without requiring a blank-page click. Preserve source-card focus restoration, but only keyboard-visible focus pauses a collection; pointer-restored focus must not. Reconcile focus when input modality changes, and preserve saved user pauses, hidden-document pauses and reduced-motion/native-mobile behavior. Release the collection's protective startup hold on actual detail-return completion rather than leaving the fixed delay running after the transition. Preserve restored phase/direction and all approved menu styling.

- September 9 third material-sculpture set: after reviewing the continued direction, Andrew immediately requested one more complete set. Continue the approved photoreal fabricated-object language while varying the underlying construction logic: cast, woven, compressed, eroded, hinged and counterbalanced forms. Preserve tactile evidence of making and real-world forces; keep Set 3 isolated under `Project-Images/article-options/material-sculpture-set-3/` until Andrew makes article-by-article selections.

- September 9 menu pause after color comparison: restore all three menu labels to black in normal, hover and current states, superseding the white-label trial. Andrew wants to leave the menu alone for now. Keep the black icon/focus outline and every approved material, typography, spacing, geometry and motion setting unchanged; make no further menu refinements without a new request.

- September 9 white menu labels with black icon: the three expanded-menu links use fully opaque white text in normal, hover and current states, matching the white page titles. Keep the plus/X icon black in every state, including detail-close, and retain the black keyboard focus outline. This supersedes black menu text only; preserve Semibold 600, 32px desktop/24px compact sizing, 12px row gaps, solid-closed/60%-frosted-open white fill, 32px blur and the approved geometry/morph.

- September 9 one-pixel wordmark increase: the shared `A. ZELLINGER` logo is now 14px, up from 13px, on every route and breakpoint. Preserve actual Geist Semibold 600, white color, existing tracking/line-height, placement, transparent background and root link. Andrew also approved the larger, instruction-free page-title context lockups; keep that treatment as the current baseline.

- September 9 context simplification: remove the gray instructional line from every page-title context lockup, including collection scroll cues, detail scroll cues and the History end cue. Do not retain a hidden hint element or its reserved spacing. Keep the orange 12px counter, 20px item/section name and 1.5px progress rule, with the same 32px title gap and 25px name-to-rule gap. Scrolling, section tracking, progress behavior and compact visibility remain unchanged.

- September 9 context-scale trial: increase the shared page-title context counters and scroll cues to 12px/18px Geist Mono Regular 400, and item/section names to 20px/28px Geist Medium 500. The progress track and its orange fill are now 1.5px thick, a modest increase from 1px; retain the 96px width and existing scroll animation. Apply consistently to Projects, Articles, History and both detail states. Preserve the 32px title-to-context gap, 25px text-to-rule gap, internal spacing, colors, tracking, large page titles and compact/short-height hidden treatment. This is a visual trial for Andrew's next review; use shared context sizing variables rather than changing unrelated card metadata or body typography.

- September 9 continued material-sculpture approval: Andrew called the first material-sculpture article-image set excellent and authorized another 20-image expansion. Preserve the photoreal physical-sculpture direction while widening the material and construction vocabulary without repeating a single signature motif. Favor real glass, wax, latex, felt, concrete, brass, charred wood, linen, paper pulp, cork, porcelain and related materials with believable gravity, contact, imperfections and studio shadows. Keep this continuation isolated under `Project-Images/article-options/material-sculpture-set-2/` pending image selection; do not publish an active article image yet.

- September 9 modest desktop-control increase: Andrew specified 92×92px for the circular menu/detail-close button, increasing from 88×88px and superseding the proposed 96px trial. Keep compact/short-screen controls at 64×64px, the 48px bottom inset, 16px panel-to-circle gap, current icon size and panel dimensions. Preserve the new solid-closed to 60%-frosted-open fill behavior, black foreground and 12px menu row gaps.

- September 9 state-dependent menu fill: the closed/resting circle is fully opaque white. Opening smoothly reduces the unified button/menu fill to 60% white, and closing restores 100%. Drive tint from the same reversible morph progress, including interrupted toggles and immediate reduced-motion endpoints, rather than a separate CSS transition. Keep 32px background blur, fully opaque black icon/labels, Semibold 600, 12px row gaps and all geometry unchanged. A detail-close button with its menu closed also uses the solid-white resting fill.

- September 9 final menu adjustment: restore fully opaque black plus/X and labels, superseding the white foreground trial, and reduce the menu's vertical row gap from 14px to 12px at every breakpoint. Retain the three 44px targets, existing panel envelope/padding, Semibold 600, 60%-opacity white fill, 32px backdrop blur and approved reversible morph.

- September 9 latest frosted foreground: the shared plus/X icon and all three menu labels are fully opaque white in every state, superseding both black and orange foreground trials. Keep the current 60%-opacity white fill and 32px backdrop blur unchanged, along with Semibold 600, spacing, geometry and motion. Retain the distinct black keyboard focus outline.

- September 9 frosted-menu correction: restore fully opaque black navigation labels in every state and reduce the white surface fill to 60%. Andrew rejected the orange text and the more opaque 65%/75% fills. Retain 32px backdrop blur, the single live silhouette mask, black icon, Semibold 600 labels, current spacing and reversible motion. This supersedes the orange-label and intermediate-opacity trials below.

- September 9 orange menu-label trial: color Projects, Articles and History labels brand orange in resting, hover and current-page states. Retain actual Geist Semibold 600, unchanged spacing, no selected underline and the black focus outline. The plus/X icon stays black; the shared frosted surface stays 65%-opacity white with 32px backdrop blur. This supersedes black menu text only.

- September 9 article-image material-sculpture direction: Andrew selected the first mixed-medium expansion image, a photoreal braided-fiber and speckled-ceramic construction, as the visual anchor for a new set. New article-image exploration should use photorealistic studio macro photography of physically buildable abstract sculptures. Prioritize real material weight, texture, tension, contact points, joints, imperfections and shadows across fiber, ceramic, stone, metal, glass, paper, rubber and related materials. The sculpture itself carries the editorial metaphor; avoid floating digital geometry, diagrams, UI symbolism and generic CGI. Preserve earlier sets and keep this new direction isolated under `Project-Images/article-options/material-sculpture-set/` until Andrew selects publishable images.

- September 9 frosted-menu trial: the circle and expanding menu use a white fill at 65% opacity, superseding the 75% trial that obscured too much background color and the earlier 60% trial, with a strong 32px backdrop blur. Mask one tinted backdrop layer with the existing live gooey silhouette so overlapping shapes do not double the tint or blur. Keep black icon and 600-weight labels fully opaque, and preserve all sizes, spacing, layering, bottom inset and reversible motion. This supersedes solid-white navigation only; do not reactivate the rejected refractive/displacement glass treatment.

- September 9 expanded-menu weight: Projects, Articles and History navigation labels use actual Geist Semibold 600, one step heavier than Medium 500. Do not tighten their letter spacing; retain -.02em tracking, 32px desktop/24px compact sizes, 1.2 line height, 14px row gaps, black text, white surfaces and approved menu geometry/motion. Scope the semibold face to `Geist Menu` so unrelated typography remains unchanged.

- September 9 collection hover refinement: Projects and Articles card titles retain their resting color on hover; never dim the hovered title or imply deselection. Remove the shared grey-title hover rule and its color transition. Preserve full-card links, keyboard focus outlines, orange article Read more labels and unrelated hover/motion behavior.

- September 9 persistent-control layering: the circular menu/detail-close control and its expanded menu must always paint above temporary project/article transition media and text in both directions, at every breakpoint. Keep the existing viewport-fixed shell and bottom placement; do not replace fixed positioning with page-relative absolute positioning. The shared navigation ancestor owns the top chrome layer (130), above body-appended transition media (120) and copy (121); compact layouts must not lower that ancestor's stacking level. Preserve geometry, palette and approved motion.

- September 9 History dot composition correction: Andrew rejected the .30em circle and widened .36em slot as much too large. Use .17em diameter, about 21% larger than the original .14em dot, with the original .225em horizontal slot. The intended illustration treatment is only a slight exaggeration of punctuation, not a large competing disc. Preserve the baseline/bottom edge, combined-mark centering and shared orange overlay treatment.

- September 9 History monogram: the static portrait overlay reads `A.` with a circular orange dot replacing the font's squared period. Retain the baseline and center the complete mark as one unit over the image; the newer compositional sizing direction supersedes matching the font's horizontal advance. The dot shares the A's parent overlay layer so brand color, overlay blending and 70% opacity are identical. Preserve the Monument scale, actual Geist Semibold 600 and photograph's crop. The archived Home marks remain single A glyphs.

- September 9 wordmark weight: increase the shared `A. ZELLINGER` logo one weight from Geist Medium 500 to actual Geist Semibold 600 on every route and breakpoint. The semibold face is scoped through the `Geist Wordmark` family so other typography is unaffected. Preserve 13px size, white color, tracking, line-height, position, text and root link.

- September 9 menu item spacing: use a 14px vertical gap between Projects, Articles and History link rows at every breakpoint, superseding the too-generous 16px trial and original 4px gap. Keep the three 44px link targets centered as a group within the existing panel; preserve outer dimensions/padding, 32px desktop/24px compact typography, the white/black palette and approved smoothed morph.

- September 9 menu smoothing accepted: Andrew approved the continuous organic morph after reviewing the live revision. Preserve the bud/neck/panel sequence and 900ms duration, but use one shape-preserving sampled curve with continuous pose tangents and finite used corner radii rather than separate ease-in-out segments and a clamped 999px radius. Open and close traverse the same timeline in opposite directions, including interrupted toggles. Keep the eased plus/X rotation, current staged text reveal, white/black palette, 32px desktop menu labels, all geometry and reduced-motion behavior. No further visual changes to this treatment without direction.

- September 9 desktop menu text increase: use 32px labels at widths of 992px and above, while tablet/mobile remain 24px. Preserve Geist Medium 500, 1.2 line height, black text without a selected-page underline, white surfaces, current link targets, panel geometry and reversible animation. This supersedes the all-breakpoint 24px size only on desktop.

- September 9 article detail alignment: the opening grey divider spans the full right content rail, aligned with the hero image edges. Center the existing 68ch reading column beneath it with equal side gutters; paragraphs, section labels, lists and related-work links remain left aligned within that column. Use available width when the rail is narrower, preserving typography, the 64px header-to-rule gap, 52px rule-to-body gap, internal paragraph/section spacing and header alignment. Apply to all article details without altering case studies or the divider-free Articles listing.

- September 9 divider-free Articles spacing: replace the former doubled row insets with one trailing 48px gap on desktop and 32px below 992px, with zero top padding on each listing row. The desktop loop seam uses the same 48px gap because each row owns its trailing spacing. This supersedes preserving the divider-era row padding; retain card-internal spacing, full-card links, orange unlined Read more labels, content, imagery and other routes.

- September 9 Articles listing simplification: remove every row divider from the Articles main page, including cloned carousel rows and compact layouts. Keep the existing row spacing, thumbnail/text layout and full-card links. The existing `Read more` labels use brand-orange text with no underline or bottom border, including hover; preserve their wording and typography. Article detail and other routes' dividers are unchanged.

- September 9 latest wordmark abbreviation: the shared logo now reads `A. ZELLINGER` on every page and detail view, superseding `ANDREW ZELLINGER`. Preserve its white color, current typography/placement, transparent background and root landing-page link. No other names, headings or metadata change.

- September 9 article-image exploration: generate three 3:2 abstract editorial options per canonical article, organized under `Project-Images/article-options/<slug>/`. Favor conceptual geometric, sculptural/material, and spatial/topographic metaphors over literal people, devices, interfaces, or depicted article titles. Use the site's black, warm-white, muted-gray, and brand-orange language with restrained tactile texture. Keep all options text-free and do not select or publish an active article image until Andrew reviews the set.

- September 9 menu text increase: live menu labels are now 24px at all breakpoints, superseding 22px. Preserve Geist Medium 500, 1.2 line height, black text without a selected-page underline, the heavier plus/X, white surfaces, geometry and animation.

- September 9 menu legibility refinement: slightly thicken the shared plus/X arms from about 3.93 to 5 SVG units within the unchanged 36-unit icon bounds (rendered at the same 28px size), and increase live menu labels from 20px to 22px, retaining Geist Medium 500 and 1.2 line height at every breakpoint. Remove the selected-page underline; preserve `aria-current`, keyboard focus indicators, white surfaces/black foreground, circle/panel geometry, placement and motion.

- September 9 title/context stack: position the secondary context lockup exactly 32px below each page-level title's bottom edge, retaining the same left inset. This supersedes centering the block between the title and NYC clock. Keep the title's current size/position, context typography/content, 25px text-to-progress-rule gap and live progress behavior. Apply to Projects, Articles, History and both detail states; preserve the hidden context treatment below 992px and on desktop heights of 600px or less.

- September 9 menu palette reversal: use fully opaque white for the shared bottom circle and expanded liquid menu, with a black plus/X icon, black links in normal/hover/current states and black link focus rings. This supersedes the orange navigation treatment. Preserve sizes, bottom inset, current-page underline, reversible gooey animation and all other site styling.

- September 9 case-study divider simplification: retain only the first grey rule above the first narrative section (Context for full case studies); remove every later divider within each project write-up. Replace doubled separator padding with 48px section gaps on desktop/tablet and 32px below 768px, with no trailing section padding. Preserve the first rule's existing 52px desktop/tablet and 28px phone copy inset, header-to-rule spacing, label/body columns, typography, content, and the first rule's reveal. Article and History layouts/dividers are unchanged.

- September 9 latest full-name wordmark: restore `ANDREW ZELLINGER` in white throughout all page and detail templates. This supersedes `A. ZELLINGER`; preserve the current typography, placement, transparent background, absence of strike-through lines and root landing-page link. Page titles and all other styling remain unchanged.

- September 9 desktop title size and offset: page-level titles now use 116px Geist Medium 500 and sit exactly 32px above true viewport center, still left aligned, on desktop (992px and above), including detail shells. This supersedes the 88px centered treatment. Retain .98 line-height, -.035em tracking, title-character transitions and balanced context stacks; no additional optical offset. Tablet/mobile, logo and content-heading sizes remain unchanged.

- September 9 page-title alignment reset: center desktop page-title boxes at 50% of the viewport height, retaining their existing left alignment on all top-level and detail routes. This supersedes the raised 40vh anchor and removes its residual 3px upward optical offset. Keep 88px Geist Medium 500, tracking, line-height and title-cascade transforms unchanged. Context stacks continue to balance between the title and NYC clock; tablet/mobile retain their existing document-flow placement.

- September 9 abbreviated wordmark: the persistent shared logo reads `A. ZELLINGER` in white on every page and detail view. Preserve its typography, position and root landing-page link, with no background fill or strike-through lines. This supersedes the full-name wordmark only; page titles, metadata and biography copy retain their existing names.

- September 9 project-order update: Avantos leads the Projects collection, followed by Amazon Fire TV. Preserve the relative order of every remaining project. Use the canonical `projectDisplayOrder` so the carousel, context counter and case-study sequence remain consistent; no project content, imagery or routes change.

- September 9 page-title weight reversal: restore Geist Medium 500 for the large page titles while retaining the new 88px desktop size. This supersedes the 400-weight trial only. Tablet/mobile were already 500 and keep their existing sizes; preserve tracking, line height, positioning, transitions and all other typography.

- September 9 desktop page-title typography update: Projects, Articles and History page-level headings (including the corresponding detail shells) use 88px Geist Regular, weight 400, at widths of 992px and above. This supersedes 72px Medium desktop titles. Preserve the .98 line-height ratio, -.035em tracking, raised vertical placement, 3px optical adjustment and character cascade. Tablet/mobile retain their existing responsive sizes and weight 500; logos, collection/detail content titles and History lead are unchanged.

- September 9 identity and page-name reset: the shared wordmark reads `ANDREW ZELLINGER` in white with no orange strike-through lines or background fill. Page-level headings are `Projects` at the root landing page and case-study shell, `Articles` on the article collection/detail shell, and `History` on the biography page. Keep top-level document/share titles, title-cascade resolution and collection-return labels consistent. Preserve existing typography, positions, URLs, content-piece titles, orange navigation and History overprint. This supersedes the abbreviated logo, double-strike treatment and Selected work / Then & now / About me headings.

- September 9 double-strike wordmark: `ANDREW` is white with two thin parallel brand-orange horizontal strike-through lines across the name. Keep the background transparent and preserve its typography, header position, dimensions and landing-page link. The lines are decorative and non-interactive; apply consistently across routes and breakpoints. This supersedes the all-orange lettering and filled-label trials without changing the menu palette.

- September 9 latest wordmark treatment: render all of `ANDREW` in brand orange on a transparent background, removing the orange label fill and its added padding. This supersedes the white-on-orange label and orange-A/white-NDREW treatments. Keep typography, tracking, header inset and landing-page link unchanged on every route and breakpoint. The orange menu with white icon/text remains unchanged.

- September 9 wordmark label treatment: all of `ANDREW`, including the initial A, is white on a solid brand-orange rectangular background with 2px vertical and 4px horizontal padding. Retain the shared 13px Geist Medium type, tracking, header location and Projects landing-page link. This supersedes the orange-A/white-NDREW treatment on transparent black; apply on every route and breakpoint.

- September 9 latest menu palette: the bottom circular button and expanded liquid menu are fully opaque brand orange (`--swatches--accent-1`), with a white plus/X, white links in default/hover/current states and white link focus rings. Preserve the current-page underline, 88px desktop/64px compact circle, 48px bottom inset plus safe area, upward reversible menu animation, raised page titles and band-free mobile layout. This supersedes the previous white-background/black-foreground treatment; History's portrait and editorial accents are unchanged.

- September 9 raised desktop page titles: large page headings no longer sit at the true viewport midpoint. Center their layout at 40% of viewport height on all desktop top-level and detail routes (992px and above), retaining the 3px upward optical adjustment, 72px type and character-mask transitions. Context stacks continue to balance between the title and NYC clock. Remove the former short-desktop extra bottom padding so all desktop heights use the same rule; tablet/mobile positions, right rails and the bottom menu remain unchanged.

- September 9 mobile canvas and title refinement: the Projects page heading uses sentence case, `Selected work`, including case-study shells, metadata and title transitions. Remove the fixed black bottom band and its border on compact layouts; the existing white circular menu floats directly over page content. Preserve its dimensions, bottom inset, safe-area handling and opening behavior.

- September 9 History editorial refinement: remove every first-line indent and the offset section-label/body columns. Major section labels and their copy share the portrait's left edge at every breakpoint. Preserve all biography content, the 28px/1.25 lead, the portrait and its 70%-opacity Overlay A, page chrome and other routes. Use a consistent 16px/26px body, 12px/18px mono labels, 24px label-to-copy spacing, and 64px major-section gaps (48px on phones). Keep desktop experience and capability registers internally columned; phone entries stack flush left. Color two existing editorial elements orange: the Design practice label and email link. No new badges or decorative accents are added.

- September 9 softer History overprint: revert the History A to Overlay blending and reduce its opacity. The current trial uses 70% opacity on the artwork layer, superseding Difference. Keep the original photograph fully opaque and retain the A's orange fill, Semibold 600, sizing, centering and static behavior.

- September 9 History overlay blend update: use `mix-blend-mode: difference` for the restored History Monument A, superseding overlay blending on that image only. Preserve its brand-orange fill, actual Geist Semibold 600, static centered composition, size and photograph crop. Keep the archived Home artwork unchanged.

- September 9 wordmark color refinement supersedes the all-orange trial: the shared logo remains `ANDREW` with only the `A` in brand orange and `NDREW` in white. Keep the letters contiguous with the existing tracking, typography and position; retain its link to the Projects landing page. History's restored static A overlay stays unchanged.

- September 9 page-title naming: the large page headings are `Selected Work` for Projects (including the root landing page and case-study shell), `Then & now` for Articles and its detail shell, and `About me` for History. Use these names in top-level document/share titles and the title-cascade resolver. Keep the navigation labels Projects, Articles, and History, existing URLs, content-piece titles, sizing, and transitions unchanged.

- September 9 identity update: the persistent wordmark now reads `ANDREW` in brand orange (`--swatches--accent-1`) on every route and breakpoint, retaining its existing typography, position, and link to `/`. Restore the approved static Monument A overlay on History's headshot only: actual Geist Semibold 600, brand orange, overlay blend, original centered sizing and image crop. No animation or study controls return. The former Home artwork stays archived and unmounted. This supersedes the full-name white wordmark and History photo-only trial; the navigation button remains white with black icon and text.

- September 9 landing-page simplification: remove the separate Home/Designer page from the live site. Projects is now the canonical landing page at `/`, and the main menu contains only Projects, Articles, and History. The wordmark and project-detail collection returns lead to `/`; legacy `/projects` and `/portfolio` URLs redirect there, preserving query strings. Keep the existing Projects layout, carousel, context/progress, title transitions, detail navigation, and responsive behavior. Preserve the former Home portrait assets and inactive artwork for recovery, but do not mount a separate Home view. This supersedes earlier Home-page directions; no publishing or git push is implied.

- September 9 acceptance: Andrew approved committing the completed design-elevation work to main. The current reviewed state is the new accepted baseline; prior instructions to hold this experiment away from main are superseded. Preserve the full-name white wordmark, fixed 72px desktop page titles, 28px desktop collection/detail titles and History lead, restored Projects copy split, white bottom-center control with black foreground, coordinated rail/title transitions, contextual progress stacks, and temporarily hidden portrait overprints. Keep the earlier baseline recoverable in git and preserve the preview worktree. Publishing to GitHub or production is a separate action unless explicitly requested.

- September 9 final editorial-size adjustment supersedes the 24px trials: use 28px on desktop (992px and above) for Projects collection titles, Articles collection titles, case-study detail titles, article detail titles, and History's opening paragraph. Preserve each line-height ratio and tracking, tablet/mobile sizes, the restored Projects title-left/details-right split, and the large 72px page titles. Main remains untouched.

- September 9 project-copy split and detail-title parity: on desktop (992px and above), Projects collection cards restore the earlier two-part text arrangement beneath the thumbnail: project title in the first third of the original three-track grid, metadata and description together spanning the remaining two tracks, using the existing grid gutter and top alignment. Keep tablet/mobile stacked and retain the current photo/copy spacing and loop behavior. Both case-study detail titles and article detail titles are now 24px on desktop, matching their collection titles; preserve line-height ratios, tracking, and detail header layout. The large page titles stay 72px. This supersedes the earlier instruction to leave desktop detail sizes unchanged. Main remains untouched.

- September 9 desktop editorial-size trial: Projects collection card titles, Articles collection card titles, and the History opening paragraph all use a fixed 24px at widths of 992px and above. Retain each element's existing line-height ratio, weight and tracking; do not change tablet/mobile sizes, large 72px page titles, or case-study/article detail title sizes. Keep the trial isolated from main.

- September 9 latest navigation palette supersedes orange: the bottom circle and expanded gooey menu use fully opaque white, with a black plus/X icon and black links in normal, hover and current states. The current-page underline stays, and link focus rings are black. Preserve 88px desktop/64px compact sizes, 48px bottom inset plus safe area, the 16px panel gap and existing reversible motion. Portrait overlays remain off; desktop page titles remain 72px. No glass layer is active, and main stays untouched.

- September 9 desktop title-size trial: every large page title in the design-elevation experiment is a fixed 72px at widths of 992px and above, replacing the 68–120px fluid size. Retain the existing 0.98 line-height ratio, Geist Medium weight, -0.035em tracking, 3px optical offset, character cascade and descender clearance. Tablet/mobile keep their existing responsive rule. This supersedes earlier desktop title-size entries, without changing content headings, the logo, or main.

- September 9 final material reversal supersedes both glass trials below: Andrew rejected the clear-glass appearance and restored solid brand orange on the bottom circle and expanded gooey menu. Keep white icon/links/current-page underline, 88px desktop and 64px compact sizes, 48px bottom inset plus safe area, the 16px menu gap, and the reversible animation. Portrait overlays remain temporarily off. The glass trial is preserved in `center-refractive-gooey-surface.jsx` and `center-glass-material.js`, neither imported by the live renderer. Main stays untouched.

- September 9 restored-glass direction supersedes the 30%-white blur trial: Andrew explicitly wants the earlier clear refractive glass treatment itself, not refraction added to the white tint. Reuse `DOM_GLASS_DEFAULTS` (including frost 0 and blur 1) and its original displacement-map generator, adapted to the current circle and growing menu with local filter IDs. Retain the soft rim/shadow, fully white foreground, current geometry and reversible gooey timeline. Do not re-enable the old controller/layout or glass bands. The previous 30% white tint and heavy 32px blur are no longer the active material. Portrait overlays remain off and main remains untouched.

- September 9 glass-fill trial supersedes orange navigation: use white at exactly 30% opacity with a strong 32px CSS backdrop blur on the shared button and expanded menu. The plus/X and menu text remain fully opaque white. Use the existing live liquid silhouette as one mask for one tinted backdrop layer, preserving the gooey neck and reversible timeline without doubled tint at overlaps. Keep 88px desktop/64px compact sizes, the 48px bottom inset, and the 16px menu gap. Both portrait overlays remain temporarily disabled. This is a reversible experiment, not restoration of the old refraction/WebGL runtime; main stays untouched.

- September 9 photo-only comparison: temporarily disable both Home and History A motif overlays through `PORTRAIT_ARTWORK_ENABLED = false` in `src/elevation/portrait-artwork.js`. Preserve the static artwork source, coordinates, sizing, fonts and blend treatment for easy restoration. Keep both original photographs and their current crops, the orange navigation with white icon/text, and all other layout and motion unchanged. This temporarily supersedes the live static-overprint direction below; it is not deletion or a final rejection of those compositions.

- September 9 bottom-center navigation trial: the shared plus/open-menu/detail-close control is horizontally centered and fixed 48px above the viewport bottom, superseding the initial 24px trial, adding the device safe-area inset where present, on every route. Measure that gap from the circle's lower edge, not the larger liquid drawing host. The latest desktop circle is 88×88px (superseding 100px and the briefly suggested 80px); compact/short-screen circles remain 64px. Keep solid brand orange for the circle and growing liquid menu; the black-background idea was rejected. The plus/X icon, all links (including hover and current-page states), and link focus indicators are now white, superseding the black-foreground trial. Underline the current link. Preserve icon size, upward reversible menu morph, 16px menu-to-circle gap and accessible targets. Titles and context stacks remain at their approved positions; they no longer vertically align with the circle. Keep this trial isolated from main pending review.

- September 9 static portrait direction supersedes the animation experiment: live Home and History use fixed A overprints, with no automatic sequence, blinking, random replay or study controls. History retains exactly the large centered Monument A from the supplied 00:30:30 screenshot, using the same sizing formula and overlay blend. Home follows the ten sparse Signal positions in the supplied 00:31:22 screenshot, varying several glyph sizes while retaining the overall composition. Edge bleed is permitted inside the photo's crop; keep the left column and surrounding canvas clear. Both use actual Geist Semibold 600 in brand orange with overlay blending. Preserve photographs, crops, all unrelated site motion and main; keep the earlier animation lab source available but unmounted.

- September 9 mixed-scale addition: show large and small A's simultaneously as well as varying scale between studies. Lattice uses a nested grid with one or two reserved two-by-two large-letter tiles and smaller letters in the remaining cells; a shared occupancy map prevents overlap. Randomize the large-tile placement between takes, not while visible. Preserve hard cuts and actual Geist 600 throughout.

- September 9 portrait-grid variation: avoid repeating the same four-by-four field and letter size. Favor larger A glyphs in two-by-two and two-by-three/three-by-two compositions, vary Stack between two and three rows, use medium Lattice grids, and reserve Signal for a denser five/six-column field. Each fresh take/replay varies the grid dimensions as well as its seeded cut pattern. Size the actual 600-weight glyphs to their cells without stretching; never resize a visible composition mid-vignette. Keep the selected seven motifs, stationary binary cuts, pause/reduced-motion safeguards, both photographs and main intact.

- September 9 overprint revision supersedes the smooth portrait-study motion and the earlier 500/600 comparison: Andrew selected Stack, Lattice, Relay, Signal, Columns, Chorus, and Monument. Those seven alone form the automatic sequence. Keep their compositions/grid structure, but remove all smooth slide-in, slide-out, scale/fold exits and interpolated fades. The A glyphs stay fixed and cut directly between opacity 1 and 0, with irregular, seeded group/dropout rhythms that evoke imperfect algorithmic overprint and older technology. Use spaced partial-grid cuts and longer Monument holds, not rapid large-field strobing. Every portrait-study A now uses the real Geist Semibold 600 face, including Monument and Chorus; do not change unrelated site typography. Earlier sliced large-A tests, Rows and Orbit remain only in an explicitly earlier-tests selector group, excluded from automatic playback. Main, original photographs, crops, and route transitions remain unchanged; visual acceptance remains Andrew's.

- September 9 A-study feedback: Andrew favors Monument and Chorus's structured, grid-aligned direction; Orbit is not favored and is excluded from automatic playback (keep it only as an earlier comparison). Add ten further studies, including masked repeated copies that resolve into one complete A, coordinated rows/columns, and sparse on/off accents. The ten new studies use actual Geist Sans Semibold at weight 600, while earlier studies stay at 500 for comparison. Keep transitions eased, deterministic, mask-bound and non-bouncing; on/off cuts must be spaced and partial-field, not rapid whole-image flashes. Preserve both photographs, all unrelated routes and main. These remain motion explorations pending Andrew's visual selection, not final brand approval.

- September 9 portrait A-study is an exploratory treatment, not an accepted brand direction. Only Home's seated portrait and History's top portrait receive clipped, decorative orange Geist Sans Medium capital-A motifs: an oversized monogram, staggered repeated letters, and a spiral/orbit. Keep the original images and crop transforms intact. Provide motif, overlay/difference/translucent blend, pause, and original-photo comparison controls during this experiment. Respect reduced motion, offscreen/visibility and route-transition pauses. Keep all other routes and main unchanged. The entire study can be disabled through `PORTRAIT_STUDY_ENABLED` in `src/elevation/portrait-study.js`.

- Large page-title changes use one GSAP character cascade in a fixed clipping window: outgoing letters stagger left-to-right downward until fully hidden, then incoming letters stagger from above into their resting position with smooth non-bouncing easing. Do not translate the whole title container or alter the approved upward right-rail sweep. Preserve the heading's accessible full label, current typography, tracking, and desktop optical offset. Mask ink clearance must preserve the complete Designer g descender without moving the title or context block. Resolve the canonical route title before splitting; late Home/Projects or detail setup must not overwrite already-split letters when the label is unchanged. Reduced motion remains immediate; same-heading collection/detail transitions keep their existing behavior. Main remains untouched pending review.

- Every desktop scrolling context block includes the shared progress rule. Projects and Articles collections show position through their complete logical carousel sequence, updating continuously during ambient and user-driven motion, following reverse input, and wrapping at the loop seam. Case-study and article details show reading progress inside the active piece; History shows document progress. Home remains a static practice introduction without a progress rule. Remove the visible Articles Pause/Resume control as well as the already-removed Projects control; preserve saved pause preferences, focus/visibility pauses, and reduced-motion behavior.

- The approved desktop context treatment now applies to Home, Projects, Articles, History, and both detail states, superseding the earlier Home/Projects context removals. Vertically center the complete block between the large title's bottom edge and the NYC clock's top edge, left aligned at the existing inset. Home uses a concise design-practice introduction with no counter, scroll cue, or progress rule. Collections show the visible item's name, ordinal, and browsing cue; details add reading progress. History tracks its actual section headings and document progress. Keep the shared typography and 25px text-to-rule gap where a rule exists, omit that reserved gap when it does not, and recompute for viewport, font, and text-height changes. Preserve the existing hidden treatment below 992px and on desktop heights of 600px or less. Do not restore secondary navigation or the Projects pause button.

- In the design-elevation experiment, the persistent NYC clock uses 12px font size and 12px line height in Geist Mono on every route and at every breakpoint. Preserve its existing placement, tracking, color, timezone, and live updates.

- Projects has no supporting context block between its large page title and the NYC clock: remove the current-project count, repeated project name, and scroll hint from `/projects`. Also hide its visible Pause/Resume motion control. Preserve carousel behavior, saved pause and reduced-motion preferences, project cards, and other routes' context text and pause controls, including case-study detail context.

- Home has no supporting context block beneath its large Designer title: remove “Ideas into interfaces.” and “Explore the work” rather than replacing them. Keep other routes' context text unchanged unless separately requested.

- Desktop large page titles receive a 3px upward optical offset relative to their existing centered layout, so the title-case lettering reads level with the circular plus/X control. Apply to all top-level and detail page titles at widths of 992px and above; do not move the button, alter title animation transforms, or offset tablet/mobile titles.

- In the design-elevation experiment, the centered desktop plus/X circle is 100×100px. Compact and short-height docked controls remain 64×64px. Preserve the existing icon size, menu-panel size, reversible morph, and 16px final panel-to-circle separation; dock below 676px viewport height so the larger circle cannot push the panel into the top frame.

- The center plus/X control has no visible caption underneath it in any state or viewport. Remove the experimental Menu/Close/Back caption, while retaining accessible button names, expanded state, navigation links, and detail-close behavior.

- Remove the experimental secondary Home/Projects/Articles/History link row above the NYC clock on every route and its stacked mobile counterpart. The circular plus-button menu is the sole page-navigation menu; preserve its links, the logo's Home link, content-card navigation, detail Close behavior, and the live NYC clock.

- Large page titles have no eyebrow or metadata line above them on Home, Projects, Articles, History, or either detail view, at every breakpoint. Do not restore the experimental page-title labels or their reserved spacing. Preserve metadata within project cards, article cards, and detail content.
- In the design-elevation experiment, large page-title tracking is -0.035em at every breakpoint: a tighter middle ground after the -0.02em trial, without fully restoring the original -0.055em. Keep the current responsive title sizes, weight, line heights, and route-specific content-heading styles unchanged.

- September 8 Home-return correction: a case-study page's Home link or logo uses the same upward full-rail sweep as the top-level pages, capturing only the current reading viewport. This is a page departure, not Close detail. Preserve the existing Close/Back-to-Projects morph, detail entry and reading motion, and other detail route boundaries.

- September 8 follow-up approval: implement the mission-critical audit fixes first, then prove the Home ↔ Projects Counterflow transition in this isolated experiment. Andrew authorized specialist agents and design-plugin guidance. Use independent opposing column movement, a stable seam/control/identity, a compact-layout translation, reduced-motion fallback, and safe interruption/Back behavior. Browser visual review remains the acceptance gate; do not merge main or roll out unrelated transition families without that review.
- September 8 motion review supersedes the route-specific T1/T2 proposal: all distinct top-level transitions among Home, Projects, Articles, and History use the successful full-right-rail sweep as one system. The outgoing right rail travels UP out of view and the incoming rail travels UP from below; Projects/Articles then continue upward at ambient speed unless explicitly paused. The left title field moves in the opposing direction with the same timing for every pair, including Back/Forward. Do not alternate directions or substitute fades/cross-cuts by destination. Preserve stable chrome, current long-page reading position, mobile/native and reduced-motion fallbacks, and existing collection/detail reading transitions. Keep this experiment separate from main until Andrew approves it.

- Motion direction for the next experimental pass: Andrew wants a more ambitious, coordinated system with independent left/right column movement, opposing horizontal or vertical directions, full-column/image entrances and exits, and geometric bar/mask transitions. Preserve the split layout as the organizing structure. Audit and propose the transition collection before implementing this next pass; the current design remains isolated from main pending review.

- In the design-elevation experiment, Andrew is comfortable with the mobile menu overlaid at the bottom. Start with a smaller bottom-centered control and refine its visual treatment; do not presume a side-pinned control is preferred. The experiment remains subject to his visual review before any merge into main.

- September 8 design-elevation experiment: Andrew authorized a complete visual and interaction refinement in this separate worktree and `codex/design-elevation` branch. The accepted rollback baseline is main at `700ba4b`; never merge this experiment into main without his explicit review and approval. Keep the existing desktop 50:50 split, black canvas, Geist typography, content, and approved imagery as the foundation. All retained routes, components, transitions, and responsive states may be refined. Give tablet and mobile deliberate layouts. Keep the accepted preview on 5178 and this experiment on 5179.

- The September 8 Geist-wide typography experiment supersedes any remaining serif directives: no visible text uses Times Newer Roman. The History career lead, generic detail lede, and generic detail-section prose now use Geist Sans Medium while preserving their existing sizes, line heights, tracking, capitalization, and layout. Keep the unused serif font asset and display token available only for a reversible rollback until Andrew accepts or rejects the experiment.

- The September 8 full editorial pass supersedes the earlier three-case pilot and the three-section/325-word minimum rules. All 18 sufficiently documented cases use Context, Work, Key decisions (three semantic bullets), Outcome, capped at 350 body words without a minimum. Amazon Fire TV remains a short factual engagement record until Andrew supplies its decision evidence. Keep canonical summaries shared by listing and detail. Section labels are H3 elements styled to preserve the existing label geometry.
- Articles now lead with The Constraint Was the Brief, Intention Deficit Disorder, and Company of One; all 11 remain accessible. Showing My Teeth is retained verbatim at the end pending an explicit publication decision. Never merge paragraphs solely by count. Preserve authored lists and unindented quote semantics, two-line collection descriptions, and existing motion/spacing.
- Article metadata shows only a fixed, user-authorized provisional publish date and the computed reading time. It omits the redundant author name and the word “Reviewed.” The eleven dates are one-time arbitrary selections from October 2023 through September 10, 2026; never regenerate them at runtime or present them as verified historical publication dates. Read times remain computed at 200 words/minute. Project/History date conflicts must not be guessed away; the private confirmation list is docs/content-claim-ledger.md.
- History and search/share text lead with hands-on product design leadership; independent AI application work adds implementation depth, not a retirement narrative or an implication of continuous paid employment. Client breadth includes direct and studio engagements. Preserve the full experience register pending verified date corrections.

- Approved editorial pilot: Audible Sleep, Avantos, and Foursquare Brand use Context → Work → Key decisions → Outcome, with three semantic decision bullets replacing repeated Work prose. Preserve their current dates, summaries, Context and Outcome until factual review. Other case studies retain three sections pending pilot approval. Correct clear brand-name typos without changing URLs. Article import repairs restore source openings and semantic rubric/framework structure; disputed claims, publication dates, and article removal remain a separate decision.

- Andrew confirmed the previously unnamed screenshot experience entry as Greater Than One, Director of UX (Contract), 2020–2021. Include it in History; this resolves the pending organization confirmation in the positioning note.

- History positioning: this site presents Andrew's selected commercial design work; a separate site is being developed for his current and future AI projects. Use first-person biography copy connecting commercial design practice with independent AI research and application prototyping, without a tenure count such as "two decades" in the introduction. Include the identifiable experience entries and tools supplied in the screenshots, along with education and a fuller account of the evolving practice, using the existing editorial styling rather than screenshot cards. Keep senior individual-contributor, lead product design, AI-focused, fractional, and project opportunities open. Describe prototypes accurately; do not frame commercial work as obsolete or independent study as paid employment. Existing 2026 resume dates take precedence over conflicting screenshot dates. The screenshot's unnamed "Director of UX (Contract)" entry remains unpublished pending organization confirmation; omit conflicting education years. Resume editing is a separate task.

- The After Hours visual experiment was a capability test and is excluded from the active site. Keep it preserved on `experiment/after-hours`; continue the accepted design on `main` and do not reintroduce the experiment unless Andrew requests it.

- Preserve the inherited structural template while allowing approved content and menu changes.
- Preserve its typography, solid-black theme, responsive behavior, and interaction language unless the user explicitly changes direction.
- The persistent upper-left site header reads “ANDREW ZELLINGER” in white, superseding the abbreviated initial and extra-space treatments. Apply on every route and at every breakpoint. Preserve its current Geist Medium typography, tracking, placement, behavior, and canonical Home link at `/`.
- The persistent bottom-left live clock uses `America/New_York` local time and is visibly prefixed with `NYC ` on every route; preserve its typography, placement, and one-second updates.
- Do not restore the grain/noise overlay or divider lines between Home-page projects.
- Photography, Video, Discography, Projects, and Archive are intentionally removed top-level sections. Do not restore their routes, menu links, or page-specific assets.
- The canonical `/history` route uses “History” as its menu label, document title, and visible page heading; legacy `/info` and `/biography` redirect to it, while the underlying biography-content modules remain unchanged.
- History uses the restrained 3:2 profile outpaint at `public/images/history/az-headshot-extended-v1.png`, with modest additional headroom and lateral negative space. Preserve the original `public/images/history/az-headshot.png`, retain `az-headshot-extended-v2.png` as an inactive frontal alternative, and keep the established portrait frame free of distortion. The former 98-row Archive-derived reference register, its dividers, generator, and dedicated styles are intentionally removed; do not restore that list, Archive links, hover imagery, or hover behavior.
- History editorial content is a static first-pass synthesis of Andrew's 2026 resume, with AZ_1 and AZ_2 used only as supporting sources. Keep private resume provenance in source comments, avoid runtime PDF dependencies and public resume links, treat 2026 as primary, and do not publish conflicting dates or uncorroborated quantified outcomes.
- History is an editorial résumé inside the shared split-screen shell. Its top section vertically stacks the approved unindented two-sentence career headline at 32px/40px serif spanning the full right content column on desktop, a left-aligned subdued-gray uppercase 12px/13px Geist Mono `DESIGN PRACTICE` label, and the single approved combined practice paragraph at 80% of the right-column width on desktop in the Projects-description treatment: white uppercase 13px/22px Geist Sans. The practice paragraph returns to full width below 992px, and no divider follows it. Do not restore the separate supporting line or former Current Practice block. Other supporting prose uses white sentence-case 13px/24px Geist with -0.01em tracking; genuine section labels use subdued-gray uppercase 12px/13px Geist Mono on the same tightened body-column start as detail content. Experience is a full-width right-rail register introduced by an unbracketed subdued-gray uppercase 12px/13px Geist Mono `EXPERIENCE` tag with 32px of explicit bottom padding before the first job divider, followed by all 13 organization/role/date entries in three disciplined desktop columns. Organization names use that same white uppercase 13px/22px Geist Sans treatment; desktop rows keep 16px symmetric padding and subtle dividers, while rows below 992px retain 24px padding and stack fields on mobile. Keep the final job-row boundary but no additional Experience-section divider before Capabilities. The Capabilities section has 64px of symmetric top and bottom padding; its four description blocks use the shared white uppercase 13px/22px Geist Sans treatment. Selected Clients is a separate full-width row spanning the History right rail: its label aligns to the outer left edge in subdued-gray uppercase 12px/13px Geist Mono, and its unindented complete ordered list uses white uppercase 13px/22px Geist Sans with regular weight and zero tracking across the full available width. Work With Me is a single-column vertical stack with its subdued-gray uppercase 12px/13px Geist Mono tag and unindented white uppercase 13px/22px Geist Sans paragraph aligned to the same outer left edge, separated by the established 24px editorial gap. Preserve the 44px portrait-to-lead gap, 52px general major-section padding, and the 40px desktop separation before Experience; below 992px that separation remains 64px. Preserve source order and all existing factual copy. Do not restore resume descriptions, cards, hover media, the removed long reference list, or the former Independent Consulting / Studio & Leadership Foundation sections.
- `/articles` is a retained top-level section.
- `/articles` is presented as “Articles” in navigation and “Articles” as its visible page-level heading. It is the editorial sibling of the Projects collection, with no collection-count label and no divider above the first visible article row. Each desktop/tablet row uses a neutral 3:2 thumbnail at left and reuses the Projects card text-block hooks in title → metadata → excerpt order: 24px/24px medium source-case Geist Sans; 12px/13px uppercase #9c9c9c Geist Mono tags with 24px inter-tag gaps; and 13px/24px white uppercase Geist description copy. Article titles never exceed 80% of their text column. Every listing description is a purpose-written one-sentence overview of the complete article rather than an excerpt from its opening, and it must not exceed two rendered lines. A visible uppercase, non-underlined Geist Sans `Read more` text label sits 24px below each description inside the existing full-card semantic link; do not nest another interactive link or reduce the card hit area. Title-to-meta spacing is deliberately more generous than meta-to-description at every breakpoint: 16px versus 12px on desktop, 24px versus 16px on tablet, and 32px versus 25px on phone. Treat that complete text stack as one unit and vertically center it against the thumbnail without clamping multi-line titles. Desktop thumbnails stay 288px wide with a 32px text gap and 48px symmetric row padding; tablet thumbnails are 224px with the same 32px text gap and 40px symmetric row padding. At phone widths, each 3:2 image expands to the full card width above its text with a 24px gap and 32px symmetric row padding; do not carry the horizontal-row centering behavior into this stacked layout. Every row owns its bottom divider so the desktop circular seam remains continuous without introducing a divider at the top of the collection. Mobile remains native/static; preserve the approved desktop loop behavior described below.
- `/projects` is presented as “Projects” in navigation and “Projects” as its visible page-level heading; legacy `/portfolio` redirects to it. It uses one static lead image per project, no numeric project labels, and no “Discover more” links. Each image, project name, tags, and description forms one indivisible card in the same seamless GSAP loop and dramatic scroll-impulse system as `/`; keep every original and inert duplicate card contained inside the right media lane. Consecutive cards and desktop loop-set boundaries use the route-specific 32px `works-card-gap`. Every image-to-copy gap is exactly 32px. Listing titles are 24px/24px Geist Sans Medium in source capitalization with 0.01em letter spacing, tags are uppercase 12px/13px Geist Mono, and descriptions are uppercase 13px/22px Geist regular. Project-card descriptions are visually clamped to two rendered lines at every breakpoint while the full semantic text remains in the DOM and continues to render without clamping on case-study detail pages. The tag-to-description edge gap is 12px on desktop through the card-specific `works-meta-spacing` hook; phone spacing remains 25px and the shared tablet value remains 16px.
- Global page-level titles use authored sentence case with no uppercase transform and Geist Sans Medium with zero letter spacing at the centered-left position: 80px/80px on desktop with the existing responsive tablet/mobile sizes retained. Case-study and article title headings use the same Geist Sans Medium family while retaining their route-specific sizing and tracking. The persistent NYC clock uses 13px/12px Geist Mono in #9c9c9c.
- `/` is the canonical Index/home route. Its navigation label is “Home” and its only intro text is the heading “Designer” at every breakpoint; legacy `/index` redirects to `/`. Do not add supporting subtext beneath it, and keep the heading at the Projects title position.
- `/` uses one fixed right-rail portrait at `public/images/home/az-hero-extended-v2.png`, the second AI-assisted 4:5 outpaint of the user-supplied `public/images/home/az-hero.png`, with no carousel roots, loop track, scroll spacer, controls, slide UI, numeric labels, project names, tags, descriptions, or “Discover more” links. Desktop first cover-fills the entire rail, then scales that covered surface to 110% from its center, creating the approved middle ground between the first and second outpaint figure scales without exposing rail gaps at wider aspect ratios; below 992px it returns to a static 3:2 frame in normal flow. Preserve the original and first outpaint alongside this version so Andrew can replace or roll back the provisional crop without changing the layout.
- Retained non-motion routes use GSAP core for eased wheel/keyboard scrolling while touch remains native; `prefers-reduced-motion: reduce` disables artificial smoothing globally. History explicitly uses fully native scrolling below 992px. Home is fully native and static at every breakpoint. On desktop, `/projects` uses a perpetual seamless bidirectional loop inside one fixed, overflow-clipped media viewport. Its source items and two aria-hidden duplicate sets share identical geometry and remain contained in the media lane. The loop keeps moving in the screen-space direction of the user's latest vertical wheel, keyboard, touch, or native scroll input. Scroll input over the media lane applies an immediate, dramatic velocity burst that smoothly decelerates back to the 60px/s ambient speed while preserving the latest direction; repeated input refreshes the boost. At widths below 992px, Projects instead uses native scrolling with only its logical source items rendered statically in normal document flow: no loop track, duplicates, impulse, or artificial easing.
- Browser scrollbars stay visually hidden on every route while document scrolling remains enabled for wheel, trackpad, keyboard, and touch input. Do not implement this with `overflow: hidden` or alter horizontal overflow behavior.
- The global outer canvas frame is 24px on desktop across top, right, bottom, and left; tablet and smartphone frames remain 16px. Keep that outer-frame token separate from the existing 16px internal grid, card, and loop gaps.
- `/` has no repeated images or Index loop spacing; preserve the responsive outer canvas inset around its single portrait without changing `/projects`.
- Retained portfolio and editorial media surfaces use the local Tasman Glacier photograph at `public/images/tasman-glacier-stand-in.jpg` as a reversible stand-in until each surface receives approved replacement media. History uses `public/images/history/az-headshot-extended-v1.png` while preserving the original portrait and inactive frontal alternative, and Home uses the provisional second outpaint at `public/images/home/az-hero-extended-v2.png` while retaining both earlier source versions. Preserve every other route's established 3:2 geometry and motion, and do not restore or reference the removed legacy project/page raster imagery.
- Repo-local source images live in `Project-Images/`; browser-served project images live in `public/images/projects/`. Audible Sleep, Android Wear, Fi Collar, WeWork Studio, Foursquare Brand, Live Auctioneers, Proctor & Gamble, Thompson Reuters, PwC Audit, Avantos, Turner Media, McDonalds Kiosk, NW Mutual, Amazon Fire TV, Andrew Eccles, Modern Age, Positive Brand, Obagi Care, and Gero Timer have approved replacement media. Their collection cards and case-study details use `public/images/projects/audible-sleep-screen-array.png`, `public/images/projects/android-wear-array.png`, `public/images/projects/fi-screen-array.png`, `public/images/projects/wework-screen.png`, `public/images/projects/foursquare-branding.png`, `public/images/projects/liveauctioneers-screen.png`, `public/images/projects/proctor-screen.png`, `public/images/projects/reuters-screen.png`, `public/images/projects/pwc-website.png`, `public/images/projects/avantos-screen.png`, `public/images/projects/turner-media-screen.png`, `public/images/projects/mcdonalds-kiosk.png`, `public/images/projects/nwmutual-screen.png`, `public/images/projects/amazon-fire-tv.png`, `public/images/projects/andrew-eccles-screen.png`, `public/images/projects/modern-age-screen.png`, `public/images/projects/positiveintelligence-screen.png`, `public/images/projects/obagi-screen.png`, and `public/images/projects/gero-screen.png`; other retained media continues using the Tasman Glacier stand-in until replaced. The removed Juuice App, Tred Auto, and Seattle Genetics image files remain preserved locally for possible recovery but are not active project media.
- All project, article, Index, History, and detail media surfaces must fill their assigned frame without distorting the source image. Cropping is acceptable and expected; stretching is not. All site content now stays native DOM paint because the top and bottom WebGL glass bands are disabled site-wide.
- Projects and case-study content is a static one-time extraction from Andrew's Notion portfolio gallery plus user-supplied project material, stored locally in `src/project-content.js`; the live site must never fetch, sync, authenticate with, or write to Notion. Keep these 19 records in the approved display and circular-detail order: Audible Sleep, Turner Media, Obagi Care, WeWork Studio, Android Wear, Live Auctioneers, Andrew Eccles, Proctor & Gamble, Modern Age, Fi Collar, Thompson Reuters, Gero Timer, Foursquare Brand, Amazon Fire TV, PwC Audit, NW Mutual, McDonalds Kiosk, Avantos, Positive Brand. This sequence intentionally separates the dense Audible Sleep, Android Wear, and Fi Collar phone-array thumbnails, including across the circular loop boundary. Amazon Fire TV is a user-authorized provisional record grounded only in the verified History entry (Sketch, Design Lead, 2024–2025); its approved client tag is `AMAZON`, while its studio remains `Sketch`. Its short transparent narrative is intentionally exempt from the 325–410-word editorial band until supporting case-study material is supplied. Andrew Eccles, Modern Age, Positive Brand, Obagi Care, and Gero Timer use Andrew's supplied project recollections, conservatively normalized into the shared three-section public format without invented metrics, testimonials, team sizes, or launch claims. Andrew Eccles is recorded as a 2016 Crate engagement and Modern Age as a direct 2021 Design Lead engagement. Turner Media's approved client tag is `TURNER`. Gero Timer's approved client tag is `USTWO`; its separate studio metadata remains `ustwo`. Obagi Care's approved year tag is `2020`; Positive Brand's approved year tag is `2018`. Juuice App, Tred Auto, Seattle Genetics, ustwo, and TalentAI are intentionally excluded from the collection and detail sequence.
- Published case-study narratives use exactly three text sections: Context; Work, containing the complete Role and Approach text merged into one semantic paragraph; and Outcome, containing the complete Outcome and Reflection text merged into one semantic paragraph. Preserve 325–410 body words per project and the full one-time extraction as private provenance, but expose only the source-grounded normalized layer from `src/project-narratives.js`. Keep all five underlying narrative beats intact in their original order and wording, pad no thin records with invented claims, and publish no unsupported metrics. Each case study has only its primary/top project image; do not add Process media or another supporting image.
- Projects and Articles have stable detail URLs at `/case-studies/<slug>/` and `/articles/<slug>/`. List cards are semantic links. Detail routes keep the shared chrome, rotate the center plus into an accessible Close-detail X, show complete entries sequentially in the right rail, and use manual circular document scrolling on motion-capable desktop with inert duplicate sets. Mobile and reduced-motion detail views render one static source set with native scrolling. When a project detail has scrolled to another case study, Close and browser Back return through that active project's matching Projects card and pre-align the collection loop to the closing visual position. Closing Article Detail must always restore a populated `/articles` collection at its originating scroll position, regardless of detail scroll depth or circular-loop position; if detail state is orphaned during Back, force-clear the stale detail chrome, reveal the Articles container, and rebuild its loop, with a clean collection reload as the final fallback. Direct-loaded details fall back to their collection. Project detail copy comes from the static Notion extraction. The eleven authored article records live in `src/article-content.js` as the shared index/detail source of truth: “Company of One,” “A Free Surf Lesson,” “Showing My Teeth,” “Intention Deficit Disorder,” “Two-Dollar Bill,” “You Always Let Yourself Win,” “Cut, Defer or Build,” “Product Design Principles,” “Embedded Product Design Lessons,” “The Constraint Was the Brief,” and “What Makes a Real MVP?” Each record exposes exactly two shared metadata values on both the collection and detail page: a fixed user-authorized provisional publication date between October 2023 and September 10, 2026, and an uppercase `N MIN` reading-time estimate computed from the final article body at 200 words per minute and rounded up. Author and review-status labels are intentionally omitted. Their private structural-analogue notes and names are excluded from the published site. Article bodies accept arbitrary ordered paragraph and authored-heading blocks without fixed editorial categories.
- There is no generic or alternate `Detail` page state. The shared detail shell defaults visibly to `Projects`, runtime chrome derives from the canonical collection kind rather than inheriting shell text, and interrupted resize, reduced-motion, or route rerenders must atomically discard the pending detail before restoring every collection node. Returning to `/projects` always yields `Projects` with the complete Projects stack visible; returning to `/articles` always yields `Articles` with the complete Articles index visible.
- Project collection-to-detail navigation uses the approved vertical-expansion GSAP treatment without changing the split-screen layout. The activated Projects card is the canonical visual lockup: its 3:2 image and existing title, metadata, and description travel together to the 16px top canvas inset, using one coordinated 1.2s `power3.inOut` move, and that same native lockup is reused unchanged in the expanded case-study state. Do not render, clone, crossfade, or stagger a second project text lockup. The card's Context, Work, and Outcome body begins opening beneath that canonical lockup late in the travel, creating a controlled overlap rather than a route snap. Native and transition media must never appear doubled. Between adjacent project case studies, scrolling continuously and reversibly drives the outgoing canonical lockup fade/lift and image scale/shade plus the incoming image mask/scale and late canonical title/meta/description reveal. Incoming media stays fully pre-masked while below the viewport so the compositor cannot paint an unmasked edge frame before the scroll handler runs; its progressive mask begins at the viewport edge and resolves by 65% of boundary progress, before the copy enters. Do not pin, snap, animate body paragraphs as part of the boundary transition, or add ScrollTrigger. The initially expanded card is excluded from incoming-boundary motion until the user scrolls into adjacent content. Close or browser Back first collapses the active case-study body, then reverses the active image into its matching pre-aligned Projects card while the complete Projects stack fades back around it. All project surfaces stay native DOM paint throughout; rebuilt loop clones still remove any stale glass-ready classes defensively. If a card is unavailable, navigation is interrupted, or the detail was loaded directly, use the clean non-morph fallback and leave no hidden nodes or transition overlays. Below 992px and under reduced motion, use the native static-set fallback without route overlays, masks, scale, parallax, or stagger. Article collection-to-detail entry expands only the selected thumbnail while a fixed copy of the listing text fades immediately; the native detail header stays hidden until the image travel is essentially complete, then fades and rises into place so text never passes behind the image. Within Article Detail, the opening prose and every authored heading-plus-prose group reveal once on viewport entry with a restrained 12px rise and opacity transition; do not split individual lines or add divider animation. At long-scroll article boundaries, animate only the incoming article image and header while the completed article stays stable. Close fades the detail view before restoring a fully populated Articles loop, using a clean collection reload if the index DOM is unavailable.
- Every project case-study detail keeps the visible shared page heading “Projects,” matching the Projects collection across collection-to-detail navigation. Every Article Detail keeps the visible shared page heading “Articles,” matching the Articles collection. Keep route-specific browser titles intact.
- Case Study and Article Detail intentionally omit Previous/Next pagination because continuous circular scrolling is their navigation model.
- Project case-study headers reuse the complete Projects-card lockup: one 3:2 primary image; a detail-only 64px image-to-copy gap that matches the 64px description-to-Context gap while the Projects collection retains its 32px gap; the same desktop three-column grid; a source-case 24px/24px Geist Sans Medium title; subdued 12px/13px uppercase Geist Mono metadata; and the exact collection description in white uppercase 13px/22px Geist, unclamped in detail. The image wrapper, copy grid, metadata wrapper, and description wrapper span their full assigned rail or grid column and must not inherit the source site's all-sided `space-small` or `space-medium` margin and padding utilities. Each uses the canonical `projectCardTags()` and `projectCardDescription()` projection. Article Detail retains its separate header hierarchy: one 3:2 image; a 44px image-to-title-box gap; a source-case 32px/32px Geist Sans Medium title; subdued 12px/13px uppercase metadata; the exact 13px/24px collection description; and a 64px transition into its body. Do not render legacy supporting headlines or duplicate summaries. Project Context, Work, and Outcome retain their labeled divider rows and one naturally wrapping white 13px/24px Geist paragraph each. Article Detail renders ordered authored content: white sentence-case 13px/24px Geist Sans paragraphs with -0.01em tracking and 24px between paragraphs. Paragraphs have no indentation. True bullet and numbered sequences render as semantic lists with 8px between items and normal list indentation. Within each authored section, consolidate any run longer than three adjacent prose paragraphs into two or three balanced paragraphs without changing the wording or order. Do not use the serif display face inside the article body. Keep one full-width divider immediately after the article header, with 52px from that rule to the first body content. Later authored sections use subdued-gray uppercase 12px/13px Geist Mono labels separated by a single 40px vertical gap, with no additional dividers, then 24px before their first paragraph. Do not restore generic OPENING/ARGUMENT/NOTES labels or paragraph dividers.
- On desktop project details, keep the 16px section grid gutter but make the project label column 80px narrower than the original 1:2 grid, allowing the body column to start 80px farther left while retaining its existing right edge. This is the cumulative result of two approved 40px tightening passes. Apply this grid to Context, Work, and Outcome, including circular clones. Article Detail body content instead spans the full right-rail width without reserving or rendering a label column. Below 768px, both body models use the full right-rail width and natural single-column wrapping.
- Keep exactly 72px between project case studies and 148px between articles, measured from the bottom boundary of one complete detail unit to the top edge of the next unit's primary image. Apply each route-specific token inside its detail set and across circular desktop set boundaries; do not add terminal space after the final static mobile unit.
- On motion-capable desktop project details, each Context, Work, and Outcome divider draws once from left to right as its section enters the viewport, followed by the label and complete paragraph block rising 12px with a restrained opacity stagger. Keep the choreography below 700ms, use transform and opacity rather than line splitting or layout animation, and leave mobile, reduced-motion, Projects, Articles, and History static. The initial section reveal begins only after the collection-to-detail transition completes; do not use ScrollTrigger or SplitText.
- Across Articles and History, use the same editorial type language established by Projects and detail content: Geist Sans Medium for titles, Times Newer Roman for intentional narrative/lead copy, Geist Sans for body copy, and Geist Mono for metadata, section labels, experience rows, dates, categories, and compact record labels. Preserve the existing page-specific structure and density unless explicitly revised.
- The top and bottom glass-band overlays are disabled site-wide: `src/site-motion.js` must not import the glass-surface runtime and `GLASS_BANDS_ENABLED` remains `false` in `src/effects/glass-band.js`. No glass-band canvas, text proxy, media proxy, or hidden native paint may initialize on any route. Preserve the dormant locally owned AZRAEL-derived source as a reversible reference, but do not reactivate it unless Andrew explicitly requests that scope. The persistent `ZELLINGER` logo and NYC clock remain native. Keep this site DOM-first and preserve the existing GSAP, route, history, mobile, and reduced-motion systems. Do not add AZRAEL's field, paper-folding, click-ripple, sound, or 3D plus-button work.
- The persistent center navigation uses the MIT-licensed `liquid-gooey` React effect as a decorative surface island behind the existing DOM-native controls. The surface remains fully opaque white and uses no backdrop glass, refraction, frost, rim, border, or shadow. Its closed desktop state is a 100px circle; below 992px it is 96px. Follow the approved six-frame storyboard exactly: a hidden 36px circular bud starts inside the primary control; emerges above it as a connected droplet; expands into a 96×112px same-width oval; expands again into a 176×200px ellipse; resolves into a detached 232×252px rounded panel containing only Home and Projects; and finishes as a detached 280×320px rounded rectangle containing Home, Projects, Articles, and History. The original circle stays fixed below as the close control. The final panel has 30px of visible separation from the 96px mobile control. Encode those states once in a single 900ms Web Animations timeline with the symmetric `cubic-bezier(.45,0,.55,1)` segment easing. Open plays that timeline forward and Close runs the same animation instance backward with a negative playback rate; never restore separate timer-driven open and close sequences. Keep the controller interruptible from its current progress. The liquid observer must track the authored geometry directly without adding its own spring, bounce, or second morph animation. All animated menu-surface radii remain pixel values throughout the sequence; do not mix percentage and pixel radii because the unit switch produces a visible corner flash. Link visibility and the 36px Plus/X rotation derive from the same timeline progress so their reverse timing cannot drift. Existing crisp DOM links, hit targets, ARIA, menu state, and detail-close behavior stay above the decorative silhouette. Preserve the dormant glass-lab source only as a reversible development reference; it must not be imported or mounted by the live site. Keep the top and bottom AZRAEL glass bands disabled, and do not restore the rejected backdrop-glass, liquid-metal, Drei environment-map, GLB, integrated proxy-lens, or redrawn-background treatments unless Andrew explicitly requests that direction.
- On desktop, `/articles` uses the same seamless continuous GSAP motion and dramatic, direction-persistent scroll impulse as Projects, with each full article row kept indivisible inside the clipped right rail. Mobile and reduced-motion Articles remain a static semantic list with native scrolling.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.
