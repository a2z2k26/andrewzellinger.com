# Portfolio handoff — September 11, 2026

## Afternoon checkpoint addendum

Andrew resumed work and approved a commit/push of intro-modal refinements. The morning stopping-point and sizing notes below are historical; use current source and the newest `AGENTS.md` entries for the latest design values. Desktop modal is now 464×536px with the full lockup centered. Phone is up to 344×396px (288×396px at a 320px viewport), with 52px vertical / 24px horizontal padding. The visit URL has a true 8px text gap with inherited indentation reset. Close circles are 48px; X icons are 14px standard / 12px phone with 2px strokes. Paragraph leading is 28px standard / 20px phone. Desktop and phone visit-to-close gaps are 60px and 44px respectively. All changes are confined to the modal, its tests and decision documentation. Browser sizing/fit and dismissal checks passed at 320, 375, 390, 820 and 1440px; `npm run check` passed all 248 tests and production packaging. This document is included in the afternoon checkpoint, so the earlier uncommitted-document note describes the morning state only. Hosting rollout is separate from a verified GitHub push.

## Stopping point

Andrew ended the session with: “good. talk tomorrow - write a memory and a handoff document for the next agent and heres our stopping point”. Stop implementation here. Resume with Andrew's review and next instruction, not unsolicited refinements.

The complete mobile isolated-detail checkpoint is committed and pushed:

- Checkout: `/Users/az/Claude/andrewzellinger.com`.
- Remote: `https://github.com/a2z2k26/andrewzellinger.com.git`.
- Branch: `main`.
- Commit: `b054bdb13112aba6aa6230ba82b2e653632f2b86` — `Isolate mobile case study and article reading views`.
- Local HEAD, origin/main and the live GitHub main ref matched exactly after push. Working tree was clean before writing this handoff.
- This handoff is a new documentation-only local file, not part of that commit. No site code was changed after the checkpoint. Do not claim this document is pushed until it is.
- GitHub push was verified; hosting deployment/production alias for this commit was not checked. Do not equate the two.

## Scope and user direction

- Desktop is accepted and complete for this phase. Preserve its design and behavior.
- Tablet is tertiary. Preserve it unless Andrew explicitly asks for a change.
- Mobile remains a first pass, with further visual review expected. The compact type direction is deliberate; do not automatically enlarge everything to generic defaults.
- Phone styling/interaction breakpoint is `max-width: 599px`; tablet is 600–991px, desktop starts at 992px. These are viewport widths, not device detection.
- Latest change addresses the fact that the old compact detail document contained every case study/article in a continuous sequence. Andrew approved trying isolated mobile detail pages instead. He requested and confirmed the push, but there has not yet been a detailed real-device review of this new interaction.
- Do not resurrect old experiments or reinterpret the site wholesale. `AGENTS.md` contains historical decisions; recent explicit acceptance and this scoped interaction decision take precedence over obsolete entries.

## Current mobile detail behavior

At phone widths, existing `/case-studies/<slug>/` and `/articles/<slug>/` URLs mount only the selected story. This is a dedicated full-screen reading page, not a small overlay or a nested-scroll dialog.

- Native document scrolling; no neighboring stories at the bottom and no URL advancement into another entry.
- Shared header retained. The collection page-title region is hidden, and the selected story owns an `h1` without changing its approved visual type size.
- Reading content starts with 80px top clearance. Bottom clearance is `112px + safe-area-inset-bottom`.
- The existing fixed bottom Close bar remains. Close/browser Back restore the saved collection position; browser Forward reopens the selected entry.
- Direct URLs and reloads work. A fresh direct entry closes to the appropriate collection. Escape also closes phone details.
- Entrance: 16px upward settling movement plus fade, 240ms, `power2.out`. Exit: 12px downward movement plus fade, 160ms. Reduced-motion skips this route motion.
- Resizing across the phone breakpoint switches between isolated and existing full-document modes while retaining the selected reading context.
- Desktop retains circular detail sets and its image/text expansion transitions. Tablet retains the full static detail document.
- Content, images, metadata and approved type styling were preserved. This change does not shorten the text of an individual story.

## Other mobile baseline to preserve

- Projects/Articles/History collection titles: 38px. Shared title-region padding: 132px top, 32px bottom.
- Projects card spacing: 32px. Image-to-title gap: 24px. End-of-collection padding: 80px.
- Project collection cards hide role/year metadata on phones; detail views retain it.
- Card/detail titles: 14px. Sustained reading text: 14px. Descriptions/excerpts/modal supporting copy: 12px. Mono labels/metadata: 11px. History lead: 20px.
- Header wordmark and clock: 12px. Header background is transparent. Difference blending belongs to the whole fixed header stack so it can composite against underlying content.
- Bottom navigation is a full-width drawer, not a floating circular button. Collapsed bar is 56px plus safe area, with a two-line handle and no visible “Menu” label. Tap/swipe up opens Projects/Articles/History; tap again, swipe down, outside tap or Escape closes. Page scrolling remains available while open. Nav labels are 12px and targets are at least 44px high.
- Detail Close is separate: 68px plus safe area, 16px label and 14px orange X.
- Intro modal mobile: max 344px wide, 16px outer side clearance, content-sized height, 56px/24px/44px padding, 24px wordmark, 12px supporting copy, 68×38px globe, 44px close target. Visit-to-close gap is 44px.
- Intro modal URL displays `www.frgmnt.ai` and links to `https://www.frgmnt.ai`. Preserve its dark gradient, subtle stroke and globe animation. Scrolling is locked while it is open; desktop carousel animation remains active behind it.

## Code map

This is a Vite multi-entry HTML/DOM-controller site. Do not assume the leftover React scaffolding is the active application.

- `src/detail-state.js`: route/history lifecycle, single-story selection, phone entrance/exit, reading anchors and collection return.
- `src/detail-state.css`: shared detail styling and phone isolated-page overrides.
- `src/detail-content.js`, `src/project-content.js`, `src/article-content.js`: canonical shared content. Do not duplicate or rewrite content to create phone routes.
- `src/detail-route-transition.js`, `src/detail-boundary-motion.js`, `src/detail-section-motion.js`: existing desktop/detail motion.
- `src/elevation/styles.css`: approved responsive typography and collection spacing.
- `src/elevation/reading-anchor.js`: semantic reading-position support.
- `src/site-navigation/index.js`, `src/site-navigation/styles.css`: drawer, shared navigation and detail Close.
- `src/welcome-preface.js`, `src/welcome-preface.css`: introduction modal.
- `scripts/check-browser.mjs`: production-browser suite, including isolated phone detail regressions.
- `tests/detail-route-transition.test.mjs`: route lifecycle assertions adjusted for interrupted-entry scroll restoration.

The last implementation commit changed five files: `AGENTS.md`, `scripts/check-browser.mjs`, `src/detail-state.js`, `src/detail-state.css`, and `tests/detail-route-transition.test.mjs`.

## Verification and limits

Completed before commit, with no subsequent implementation changes:

- `npm run check`: production build/package checks and all 248 unit tests passed.
- `npm run test:browser`: all 45 production-browser checks passed, exit 0.
- Phone checks at 320px and 390px covered both project/article selection, one mounted story, no horizontal overflow, stable URL at the end, final text clearing Close, exact collection scroll restoration, Back/Forward, direct URLs, reload, and switching to 820px tablet and back.
- Immediate browser Back during entry and reduced-motion/Escape were verified.
- Desktop autoplay/reverse, collection/detail return, native masthead transitions, compact navigation and all direct content routes passed.
- Visually inspected phone article and case-study start/end screenshots.
- Tests used Playwright Chromium, including mobile emulation. Real iOS/Safari, physical-device gestures, phone landscape and a complete accessibility audit remain unverified. No claim of comprehensive platform QA.
- A dev-server run failed the native desktop rail/title transition assertion. The same assertion passed against the built production server. Use the default built-server browser suite for this check, not `BROWSER_BASE_URL` pointed at Vite.

Temporary evidence (may not survive restart): `/tmp/portfolio-isolated-check.log`, `/tmp/portfolio-isolated-production-browser.log`, `/tmp/portfolio-browser-check/`, `/tmp/mobile-isolated-article.png`, `/tmp/mobile-isolated-project.png`, `/tmp/mobile-isolated-project-end.png`.

## Preview and next session

- Last QA Vite preview was started at `http://127.0.0.1:5178`; the in-app browser was open to `/articles/company-of-one/`. Use a phone-width viewport to see the new isolated presentation. Recheck server availability next session.
- The earlier user preview was on 5175. Do not kill unrelated existing servers. Start a preview yourself if needed, using this checkout explicitly.
- The ambient Codex cwd may be `/Users/az/Claude/caverzasio-clone`, which is not this target checkout. Check path, branch, status and HEAD before any command that mutates files or Git.
- First invite Andrew's real-device reaction to the isolated reading experience. Projects established the mobile baseline; likely remaining review areas are Articles, History, detail typography/spacing and a final modal pass. These are possible next areas, not authorization to change them automatically.
- Future commits/pushes require Andrew's direction. Preserve pending user changes and distinguish local edits, pushed commits, automated checks, deployment and visual acceptance.
